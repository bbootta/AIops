# AI 경영시스템(AIMS) 정책 — ISO/IEC 42001:2023 기준

리스크관리 에이전트 팀(`.claude/agents/`)과 산출 엔진(`risk_lib`)에 적용되는
AI 경영시스템 정책. 각 에이전트 정의의 「AIMS 거버넌스」 섹션은 이 문서를
구체화한 것이며, 충돌 시 이 문서가 우선한다.

## 1. 적용범위 (조항 4)

- **대상 AI 시스템**: 리스크관리 에이전트 팀 (orchestrator + 전문 에이전트 9 +
  검증자 + 내부심사자) 및 이들이 호출하는 `risk_lib` 산출 파이프라인.
- **용도**: 은행 리스크 산출(RWA/BIS/ECL/스트레스/ALM/ICAAP 등)의 **초안 작성과
  분석 지원**. 규제보고·자본액션·한도변경의 **최종 의사결정은 항상 인간**이 한다.
- **이해관계자**: CRO/리스크위원회(결재), 현업 실무자(검토·재현), 모형검증실,
  내부감사, 감독당국(금감원).

## 2. AI 정책 선언 (조항 5.2, 부속서 A.2)

1. **인적 감독 우선**: 에이전트 산출물은 결재 전 초안이다. 어떤 에이전트도
   자본 액션, 한도 변경, 모형 채택, 규제보고 제출을 스스로 확정하지 않는다.
2. **재현가능성**: 모든 수치는 (입력 지문, seed, asof, 코드 버전)으로 재현
   가능해야 한다. 벽시계 의존 산출 금지 — 기준일은 `asof`로 주입한다.
3. **설명가능성**: 모든 headline 수치는 산출 모듈·함수·규정 조항을 audit
   ledger에 남긴다. 설명 불가능한 수치는 보고하지 않는다.
4. **검증 없는 제출 금지 (두 층 모두)**:
   - 자체검증: risk-validator를 거치지 않은 산출물은 결재 상신 불가.
     FAIL 1건이라도 있으면 종합 FAIL.
   - 상시 독립검증: 적합성검증 팀에이전트(`claude/validation-team-agent-Pw9F5`)에
     **매 작업 요청**하고, 게이트가 `적합`이 되기 전에는 결재 상신 불가.
     게이트는 fail-closed — 응답이 없으면 `응답대기`이며 통과가 아니다.
     구현: `risk_lib.validation.independent` · 절차:
     `.claude/skills/independent-validation/SKILL.md`.
5. **기준 준거**: Basel/금감원/IFRS 기준에 없는 임의 임계치·가정 금지.
   모든 가정은 출처와 함께 명시한다.

## 3. 역할과 책임 (조항 5.3, A.3.2)

| 역할 | 담당 | 책임(Accountable) |
|---|---|---|
| 총괄 조정·최종 보고 초안 | risk-orchestrator | 위임·순서·검증 강제·시정조치 |
| 도메인 산출 | 전문 에이전트 8종 | 각 산출물의 방법론·출처·재현 메타데이터 |
| 자체검증 (2선) | risk-validator | 정합성·규제·통계 체크 (산출과 분리, **같은 코드·가정**) |
| **상시 독립검증 (3선)** | **적합성검증 팀에이전트**<br>`claude/validation-team-agent-Pw9F5` | **독립 재계산·가정 도전 (개발조직과 분리된 기준셋)** |
| 내부심사 (조항 9.2) | aims-compliance-auditor | AIMS 적합성·문서화 완비성 심사 |
| **최종 결재** | **인간 (CRO/현업)** | 모든 대외 제출·리스크 의사결정 |

산출(전문 에이전트) · 자체검증(validator) · 독립검증(적합성검증 팀) ·
심사(auditor)는 서로 대체할 수 없다 — 3선 방어선(three lines) 분리 원칙.

**자체검증은 독립검증이 아니다.** risk-validator는 산출자와 분리돼 있지만 같은
코드·같은 가정으로 점검한다. 그 결과만으로 결재하면 "우리 코드가 우리 코드를
통과시켰다"가 결재 근거가 된다. 그래서 **매 작업마다** 적합성검증 팀에이전트에
독립검증을 요청하고, 응답 전에는 결재 상신을 막는다 (§2-4 참조).

## 4. AI 리스크 평가와 영향평가 (조항 6.1, A.5)

다음 **트리거** 발생 시 orchestrator는 작업 착수 전 간이 영향평가를 수행하고
결과를 최종 보고에 포함한다:

- 신규 모형 개발/재개발, 방법론 변경 (PD/LGD/ECL/스트레스 시나리오 등)
- 골든 수치 재고정(re-pin)을 수반하는 산출 로직 변경
- 신규 데이터 소스 사용 또는 데이터 정의 변경 (default 정의, DPD 기준 등)
- 산출 결과가 규제보고·공시(Pillar 3)에 직접 사용되는 경우

**간이 영향평가 항목** (각 1~2문장): ① 영향받는 이해관계자와 결정,
② 오산출 시 최대 피해(자본 과소/충당금 과소 등), ③ 완화 통제(검증 체크,
백테스트, 인간 검토 지점), ④ 잔여 리스크 수용 여부(인간 결재자 판단 사항).

## 5. 수명주기 통제 ↔ 구현 대응표 (A.6, A.7)

| ISO 42001 통제 | 구현 |
|---|---|
| A.6.2.2 요구사항·설계 문서화 | 에이전트 정의(.claude/agents) + ARCHITECTURE.md |
| A.6.2.4 검증·확인(V&V) | risk-validator 52종 체크 + PD 백테스트(HL/binomial) + 골든 테스트 572건 |
| A.6.2.6 운영·모니터링 | KRI/RAF 3-tier 임계, intraday 경보, timeseries_ledger 추세 |
| A.6.2.7 기술문서 | 모델 카드(ops 57), Explainability(ops 58), 보고서 66페이지 |
| A.6.2.8 이벤트 로그 | audit_ledger.json (수치별 산출 모듈·함수·인용 조항, BCBS 239) |
| A.7.2 데이터 출처(provenance) | RunManifest: SHA-256 포트폴리오 지문 + seed + asof + git commit |
| A.7.4 데이터 품질 | DQ 페이지(ops 25) + validator 입력 bound 체크 |
| A.8 이해관계자 정보 제공 | 경영진/실무진 2계층 보고서, 약어 주석, 영문 board pack |
| A.9.2 인적 감독 | 최종 결재 페이지(ops 52) 서명란 — 산출·검증 책임자, CRO 3단 결재 |
| A.10 제3자 | 외부 데이터(공시자료 등) 사용 시 출처·수집일 명시 (case_studies 방식) |

## 6. 부적합과 시정조치 (조항 10.1~10.2)

1. validator FAIL 또는 auditor 부적합 발견 시, orchestrator는 **부적합 기록**을
   남긴다: (발견 체크, 원인 에이전트, 근본 원인, 시정조치, 재검증 결과).
2. 시정조치 후 **반드시 재검증**한다. 기록 없이 조용히 고치고 넘어가지 않는다.
3. 동일 부적합이 반복되면 에이전트 정의 또는 risk_lib 통제 자체를 개선한다
   (지속적 개선, 조항 10.3).
4. 부적합 기록은 최종 보고의 별도 섹션("부적합·시정조치")으로 포함한다 —
   무결점인 경우 "해당 없음"을 명시한다.

## 7. 내부심사 (조항 9.2)

- `aims-compliance-auditor`가 산출 패키지 단위로 수행: 문서화 완비성
  (manifest/audit_ledger/모델카드/결재 페이지), 재현성(digest 재산출 일치),
  검증 통과 여부, 본 정책 위반 여부.
- 심사자는 산출·검증에 참여하지 않은 독립 에이전트여야 한다(자기 심사 금지).
- 심사 결과는 적합/부적합(경중 구분) 목록으로 남기고, 부적합은 §6 절차를 따른다.

## 8. RYNTA v9.0 정합 — 제품 요건과 AI 가드레일

본 하니스는 RYNTA v9.0(Risk Yard with Neural Trustworthy Architecture) BRD의
산출·증빙 계층을 구현한다. 요건 ↔ 구현 추적은 `risk_lib/rynta.py`가 보유하며
실무진 보고서 **ops/63_rynta_coverage.html**에 매트릭스로 렌더된다.

### 8-1. AI 필수 가드레일 (BRD AIG-002~012)

| 가드레일 | 내용 | 본 하니스 구현 |
|---|---|---|
| 조회 전용 (AIG-002) | 에이전트는 범위가 제한된 비변경 접근으로 시작 | 에이전트 정의 `tools` 화이트리스트 (risk-validator·auditor는 읽기 전용) |
| 제안 전용 (AIG-003) | 분석·매핑·조치를 제안하며 자동확정하지 않음 | 각 에이전트 「AIMS 거버넌스」의 인적 감독 경계 |
| 승인 우선 (AIG-004) | 중요 변경은 정책 게이트와 사람 승인 필요 | orchestrator 검증(4)·내부심사(4-b) 게이트 + ops/52 3단 서명란 |
| 최소 권한 (AIG-005) | 도구·데이터 범위를 명시하고 기록 | 에이전트 frontmatter `tools` 필드 |
| Kill Switch (AIG-009) | 실행을 범위별 중단 가능 | 런타임 오케스트레이션 계층 책임 (하니스 범위 밖) |

### 8-2. AI 자동확정 금지 목록

Agent는 다음을 **자동확정하지 않는다** — 산출·권고까지만 하고 확정은 인간이 한다:

신용등급·여신승인 · 가격·거래 · PD·LGD·EAD 등 핵심 위험파라미터 ·
ECL·충당금·회계전표 · RWA·NCR·BIS 비율 · 감독제출·공시 · 경영조치 ·
운영코드·모형 배포

이 목록은 §2-1(인적 감독 우선)의 구체화이며, 위반 시 중부적합이다.

### 8-3. 준거 기준 교차매핑

ISO/IEC 42001:2023을 주 기준으로 하되, RYNTA A–F 공통 AI Governance Baseline이
참조하는 EU AI Act(Regulation (EU) 2024/1689), EU Trustworthy AI Ethics
Guidelines, NIST AI RMF 1.0을 통제 설계 교차 참조로 사용한다. **자동 준수·인증·
법률자문을 의미하지 않는다.**

### 8-4. 커버리지 표기 원칙

요건 커버리지는 covered / partial / platform / backlog 4종으로만 표기하고,
partial·backlog는 gap을 반드시 명시한다. 미구현을 covered로 적으면 추적성
자체가 무효가 되므로, 낙관적 표기는 그 자체로 부적합이다 (§2-5 연장).

## 9. 역량과 인식 (조항 7.2~7.3)

각 에이전트 정의는 담당 영역의 방법론·규정 참조·금지 사항을 자체 포함해야
하며(위임 시 재설명 불필요), 범위 밖 요청을 받으면 수행하지 않고
orchestrator에 반환한다.
