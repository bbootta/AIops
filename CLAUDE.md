# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (`bbootta/aiops`) is currently uninitialized beyond a single `Initialize repository` commit. The working tree contains only a `.gitkeep` placeholder — no source code, build configuration, README, or tooling rules exist yet.

There is no codebase to describe, no commands to run, and no architecture to document. This file should be replaced with real content (commands, architecture, conventions) once the project is scaffolded.

## When the project is scaffolded

Re-run `/init` (or equivalent) after the first meaningful commits land so this file can be populated from actual sources: package manifests, build scripts, README, and any `.cursor/rules/` or `.github/copilot-instructions.md` that get added.

## Branch convention observed so far

Documentation work is being done on `claude/add-claude-documentation-HcJWa`. The default branch and contribution workflow have not been established.
