# 조건부 결재 — RUN-20260630-42

- 대상 실행: `RUN-20260630-42` (asof 2026-06-30 · seed 42)
- 대상 요청: `IVR-0DC4055ED5AF` (4차)
- 게이트 상태: **`조건부`** → 결재 기록 제출로 **통과 확인**
- 결재 책임자: 인간 검증 책임자
- 결재일: 2026-07-26

---

## 1. 결재 근거

| 항목 | 결과 |
|---|---|
| 독립 재계산 | **10/10 일치** (차이 0) |
| 중부적합 | **0건** |
| 경부적합 | 1건 (F-301) |
| 해소 확인 | 5건 |
| 산출값 오류 | **0건** |

4차에 걸친 독립검증에서 제기한 중부적합 4건(F-001 자본 순환 · F-002 CCR·CVA
누락 · F-003 자체검증 공백 · F-101 결함 이전)이 모두 해소됐고, 산출값 오류는
하나도 남지 않았습니다.

## 2. 인수한 잔여위험

**F-301 — request_id 지문의 잔여 구멍**

`recalc_targets`의 `citation`·`korean`이 지문에 포함되지 않아, 규정 근거가
바뀌어도 식별자가 불변이며 이전 검증 응답이 승인으로 재사용될 수 있습니다.

반증 실증: `cet1_ratio`의 citation을 변경해도 `request_id`가
`IVR-0DC4055ED5AF`로 불변이었습니다(가정 1건 추가 시에는 정상 변동).

이 위험을 안고 갑니다. 산출값에는 영향이 없으며, 규정 인용이 실제로 바뀔 때만
발현되는 조건부 위험입니다.

## 3. 후속조건 (이행기한 2026-08-09)

| # | 조건 | 담당 |
|---|---|---|
| 1 | `request_id` 지문을 요청 객체 전체 직렬화로 전환 — `asdict(request)`에서 `request_id`·`created_at`만 제외하고 canonical JSON 해시. `citation`·`branch`·`artefacts` 누락이 함께 닫힘 | 모형개발 |
| 2 | 회귀 테스트를 "어떤 필드를 바꿔도 ID가 변한다"로 일반화 | 독립검증 |

## 4. 제한 배포 범위

**`RUN-20260630-42` (asof 2026-06-30 · seed 42) 단일 실행 결재에 한정합니다.**
후속 실행·다른 기준일·다른 seed는 본 승인의 범위가 아니며 재검증이 필요합니다.

## 5. 강화 모니터링

- 다음 실행 요청 시 3선이 **지문 완전성 반증을 재수행**합니다
  (`citation`·`branch`·`artefacts` 변경 실험).

## 6. 게이트 통과 검증

결재 기록이 실제로 게이트를 통과하는지 2선 코드로 실행 확인했습니다.

| 시도 | 결과 |
|---|---|
| 본 결재 기록으로 `require(ca)` | **통과** — 조건부 결재 성립 |
| 조건(`conditions`)을 비운 기록 | 거부 — "조건부 승인 기록 미비 — 누락 항목: conditions" |
| 기록 없이 `require()` | 거부 — "독립검증 미완료 … 기록해야 결재 가능" |

fail-closed가 유지됩니다. 조건을 적지 않으면 결재되지 않습니다.

## 7. 기록 위치

- 게이트용 기록: `RUN-20260630-42.conditional_approval.json`
  (`risk_lib.validation.independent.ConditionalApproval` 스키마 대응)
- 검증팀 원장: `tools.conditional_approval` —
  `RUN-20260630-42/IVR-0DC4055ED5AF`, 조건 0/2 이행, 기한 2026-08-09
- 근거 응답: `RUN-20260630-42.response.json`
- 근거 의견서: `RUN-20260630-42.opinion.md` (4차)

## 8. 한계

- 본 문서는 인간 결재 책임자의 판단을 적합성검증 팀에이전트가 **기록**한 것이며,
  판단 자체를 대리하지 않습니다 (CLAUDE.md §7).
- 조건 미이행이 이행기한(2026-08-09)을 초과하면 본 승인의 효력을 재검토해야
  합니다. 검증팀 원장이 기한 초과를 에스컬레이션 대상으로 표시합니다.
- 독립검증 범위의 한계(비율형 표준 산출 중심, IRB·FRTB 내부모형 미포함,
  합성 데이터 기준)는 4차 의견서 §7에 기재된 그대로 유효합니다.
