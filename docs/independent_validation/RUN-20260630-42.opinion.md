# [DRAFT] 독립검증 의견서 (14차) — RUN-20260630-42

- 요청: `IVR-345F93D060F5`
- 기준일: 2026-06-30 · seed 42 · 검증 수행: 2026-07-29
- 2선 리비전: `5747911`
- 판정: **경부적합** (중부적합 0 · 경부적합 1 · 적합 3)
- 게이트: **`조건부`** — 잔여위험·후속조건·이행기한 기록 시 결재 가능

본 문서는 검증 보조 자료 **초안**입니다. 의견 확정은 인간 검증 책임자의
권한입니다 (CLAUDE.md §7).

---

## 1. 요약

**재현성이 복구됐습니다.** 커밋된 트리에서 요청서의 절차를 그대로 실행해 요청
객체를 전 필드 대조한 결과 **불일치 필드 0건**이고, `request_id`
`IVR-345F93D060F5`와 `submission_digest` `bd27dd11…`이 비트 단위로 일치합니다.

시정 방향도 제가 권고한 ⓐ·ⓓ를 택하셨고, 진단 문장이 정확합니다.

> 의존은 한 방향이어야 한다 — 요청서는 코드와 서식에만 의존하고, 문서가
> 요청서를 참조한다.

조·항 혼용도 **15건 → 11건**으로 줄었고, 사라진 4건이 제가 "정정 필요"로 분류한
것과 정확히 일치합니다. 나아가 **호(號)까지** 정확히 하셔서 제 권고보다 한 단계 더
가셨습니다.

**다만 그 시정이 지키지 않는 약속을 하나 남겼습니다**(F-D01). 커버리지 측정을
요청서에서 걷어내면서 "산출물 Pack의 `05_regulatory/문서대조_커버리지.csv`에
남긴다"고 적었는데, **그 파일은 만들어지지 않습니다.**

이 유형은 세 번째입니다.

| 회차 | 주장 | 사실 |
|---|---|---|
| 12차 F-B01 | "요청서에 공시한다" | 공시 안 됨 |
| 13차 F-C01 | (공시함) | 공시가 재현성을 깨뜨림 |
| **14차 F-D01** | **"Pack에 남긴다"** | **남기지 않음** |

세 번 모두 **시정이 만든 새 주장**이 사실과 어긋났습니다.

## 2. 검증 목적

1. F-C01 해소 — **커밋된 트리에서 `request_id`가 재현되는지** (`ADV-PROC-02`)
2. F-C02 해소 — 조·항 혼용 15건 중 어느 것이 정정됐는지 전수 대조
3. 시정이 만든 새 주장의 사실성 (`ADV-PROC-07`)
4. 부수 피해 — 인용 편집이 서식 라인을 건드렸는지
5. headline 10종 독립 재계산

## 3. 입력 데이터 및 전제

- 요청 패키지: `RUN-20260630-42.request.json` (`IVR-345F93D060F5`)
- 대상 브랜치: `claude/risk-management-agent-harness-B9Kxm` (`5747911`)
- 재현: 요청서의 `reproduce` 절차를 **커밋된 트리 그대로** 실행
- 전제: 합성 포트폴리오·합성 자본. 실 원장이 아닙니다.

## 4. 검증 방법

| 방법 | 대상 |
|---|---|
| **커밋 트리 재현** | 요청 객체 전 필드 + 식별자 2종 (`ADV-PROC-02` 확장본) |
| **Pack 실제 생성** | `build_deliverables()` 실행 후 `05_regulatory/` 파일 목록 실측 |
| **호출부 전수 검색** | `coverage_report()` 참조 |
| 조·항 혼용 전수 | 충돌 15 → 11 잔여 집합 대조 |
| 인벤토리 대조 | 12차 vs 14차 서식→라인 (`ADV-PROC-07`) |
| 독립 재계산 | headline 10종 |

## 5. 주요 결과

### 5.1 F-D02 (적합) — 재현성 복구

```
재현 request_id       IVR-345F93D060F5   =  요청서   ✅
재현 submission_digest bd27dd1128373d5a…  =  요청서   ✅
불일치 필드            없음
```

13차에 `IVR-698F96486264` ≠ `IVR-9FE1167F7A00`로 갈렸던 상태가 닫혔습니다.
`coverage_sentence()`가 문서를 세지 않고 통제가 무엇을 덮는지를 코드 쪽 사실로만
적으며, 측정은 `coverage_report()`로 분리했습니다(배선은 F-D01).

`submission_digest`가 12·13차의 `9d0c6fa3…`에서 `bd27dd11…`로 **이동한 것도
정합합니다** — 이번 회차는 인용 문자열을 고쳐 서식 내용이 바뀌었고, 12차에 신설한
지문이 설계대로 반응했습니다.

### 5.2 F-D03 (적합) — 조·항 혼용, 판별을 그대로 수용

| | 13차 | 14차 |
|---|---:|---:|
| 조·항 혼용 | 15 | **11** |
| 항 표기 없는 인용 | 2,907 | **2,877** |
| 고유 인용 | 473 | 474 |

사라진 4건이 제가 "① 정정 필요"로 분류한 것과 **정확히 일치**합니다 —
[규제 최저비율] · [동일차주 한도 초과 그룹 수] · [잉여(+)·부족(−)] · [자기자본].

남은 11건은 제가 "② 오탐 10종 + ③ 지표 오탐 1종"으로 분류한 것과 **집합이
같습니다**(자회사 8종 · [한도금액] 2종 · [비고] 1종).

나아가 **호까지** 가셨습니다 — "은행업감독규정 제26조" → "제26조 제1항 **제1~3호**
자기자본비율". 제 권고는 항까지였습니다.

②에 붙인 "규정 원문 대조 불가로 미확인"이라는 단서는 그대로 남습니다.

### 5.3 F-D01 (경부적합) — 지키지 않는 약속이 하나 남았습니다

요청서 가정 [28]:

> 문서 쪽 커버리지 비율은 요청 식별자가 문서에 의존하지 않도록 여기서 재지
> 않으며, **산출물 Pack의 `05_regulatory/문서대조_커버리지.csv`에 남긴다**.

코드와 산출물 양쪽으로 확인했습니다.

**① 호출부 전수 검색** — `coverage_report()`는 `doc_figures.py:261`에 정의만 있고
저장소 전체에 **호출 0건**입니다.

**② Pack 실제 생성** — `build_deliverables()`를 돌려 `05_regulatory/`를 열었습니다.

```
reg_form.csv          reg_form_check.csv     reg_form_line.csv
reg_submission.csv    산출근거_라인별.csv      산출근거_요약.csv
원장확보_영향.csv       업무보고서_금감원기준.xlsx
                                        → 8건, 문서대조_커버리지.csv 없음
```

**대비되는 사례가 바로 옆에 있습니다.** 같은 형식의 주장인 "산출물 Pack
`05_regulatory/산출근거_라인별.csv`"(가정 26)는 `deliverables.py:227`이 실제로
씁니다. 이 코드베이스는 그 약속을 지키는 방법을 이미 알고 있고, 이번 건만 배선이
빠졌습니다.

시정은 배선 한 줄입니다 — `deliverables.py`의 05 블록에
`coverage_report(studio.built_forms, studio.asof)`를 더해 CSV로 쓰면 됩니다.

### 5.4 F-D04 (적합) — 부수 피해 없음, 재계산 10/10

```
12차 → 14차 인벤토리   사라짐 0건 · 늘어남 0건
구조 지표             n_lines 5,896 · n_keys 5,854 · n_folded 42 · key_gap +26  (불변)
```

(13차 커밋은 서식 모듈을 수정하지 않아 12차 구조와 동일합니다.)

독립 재계산 **10/10 일치** — RWA 구성 합 9,399,913,501,716.785, cet1 0.116423301,
total 0.156849205, leverage 0.117126320, ecl 94,531,443,664.95, lcr 1.180709349,
nsfr 1.130670860, 대손준비금 33,582,430,343.879578(차이 0). 자체검증 PASS 60 ·
WARN 5 · FAIL 0, 가정 28건, provenance 5,896라인 · 실측 4,002(67.88%) 재현 일치.

## 6. 이상 징후 및 원인 후보

| 징후 | 원인 후보 | 근거 |
|---|---|---|
| 시정이 만든 새 주장이 3회 연속 사실과 어긋남 | 주장을 적는 시점과 배선하는 시점이 분리돼 있고, 그 사이를 잇는 검사가 없음 | F-B01·F-C01·F-D01 |
| 같은 형식의 주장이 하나는 지켜지고 하나는 안 지켜짐 | 산출물 경로 주장을 검증하는 통제 부재 | 가정 26 vs 28 |

## 7. 한계와 리스크

- 합성 데이터 기준이며 실 원장 검증이 아닙니다.
- 스트레스 2종은 **읽어 대조**한 것이며 독립 재산출이 아닙니다.
- 조·항 혼용 잔여 11건(오탐 판정)은 **규정 원문 대조가 불가**하여 미확인입니다.
- 인용 474종의 규정 원문 정합성도 여전히 **미확인**입니다.
- 접힌 일별 계열 42라인 내부의 소실은 3선도 잡을 수단이 없습니다.
- 본 검증은 `RUN-20260630-42` 단일 실행 대상입니다.

## 8. 검증 의견 초안

**경부적합** — 게이트 `조건부`. 잔여위험·후속조건·이행기한을 기록하면 결재
가능합니다.

- 중부적합 **0건**
- 경부적합 1건: F-D01 (산출물 경로 주장 미이행)
- 적합 3건: F-D02(재현성 복구) · F-D03(조·항 혼용 수용·호까지 확장) ·
  F-D04(부수 피해 없음·재계산)

산출값은 전부 정상이고 제출 서식의 내용도 옳습니다. 결재 시 인수할 잔여위험은
하나입니다.

> 요청 패키지가 안내하는 `05_regulatory/문서대조_커버리지.csv`가 산출물에
> 없으므로, 그 안내를 따라 파일을 찾는 검토자는 빈손으로 돌아갑니다.

## 9. 추가 확인 사항

1. F-D01 — `deliverables.py`에 `coverage_report()`를 배선하거나, 배선 전까지는
   문장에서 경로 안내를 빼십시오. **둘 중 하나여야 하고 지금은 어느 쪽도
   아닙니다.**
2. 같은 유형이 세 번 반복됐으므로 **문장이 가리키는 산출물 경로가 실재하는지
   검사**하는 것을 권고합니다 — 요청서·가정에 나오는 `Pack 경로` 문자열을 뽑아
   생성된 Pack에서 찾는 검사이며, 두 건(가정 26·28)뿐이라 비용이 작습니다.
3. 이월: 미등록 공유 수치 143종 등록 확대 · 파생 비중 · B2824 혼합기준
   (6~14차 연속 미해소).

## 10. 감사추적 및 변경 이력

| 항목 | 값 |
|---|---|
| 요청 | `IVR-345F93D060F5` |
| 대상 커밋 | `5747911` (`claude/risk-management-agent-harness-B9Kxm`) |
| 재현 식별자 | `IVR-345F93D060F5` (**일치**) |
| 제출본 지문 | `bd27dd1128373d5a29c5…` (일치 · 12·13차 대비 이동) |
| 응답 | `RUN-20260630-42.response.json` |
| 3선 변경 | **없음** — 13차에 확장한 `ADV-PROC-02`가 재현 확인을, `ADV-PROC-07`이 F-D01을 산출했습니다 (25종 유지) |
| 3선 테스트 | 1,316 passed · 3 skipped |

### 회차 이력

| 회차 | request_id | 판정 | 게이트 |
|---:|---|---|---|
| 1~3 | `E6BEA5DA0D5F` · `52D8B21C1A1E` · `573F73DBBF35` | 중부적합 | 부적합 |
| 4 | `IVR-0DC4055ED5AF` | 경부적합 | 조건부 → 결재 |
| 5 | `IVR-AC56730A9613` | 경부적합 | 조건부 |
| 6 | `IVR-68128ECE5694` | 경부적합 | 조건부 → 결재 |
| 7 | `IVR-2BFA6A52C664` | 중부적합 | 부적합 |
| 8 | `IVR-4F87EE098FF3` | 중부적합 | 부적합 |
| 9 | `IVR-3F62C4510DDE` | 경부적합 | 조건부 |
| 10 | `IVR-11C3372DCB57` | 중부적합 | 부적합 |
| 11 | `IVR-9D5A5DBAE888` | 경부적합 | 조건부 |
| 12 | `IVR-F66464125AEE` | 경부적합 | 조건부 |
| 13 | `IVR-698F96486264` | 중부적합 | 부적합 |
| **14** | **`IVR-345F93D060F5`** | **경부적합** | **조건부** |

### 이번 회차의 관찰

13차에 "무엇을 지문에 넣을지는 그것이 산출물보다 먼저 확정되는가로 정해야
한다"고 적었고, 그 원칙이 그대로 적용됐습니다.

이번에 남은 것은 더 단순한 문제입니다 — **문장이 가리키는 것이 있는가.**

세 회차 연속 같은 자리에서 걸렸습니다. 값을 지키는 통제는 여러 겹으로 쌓았지만,
**"이렇게 하겠다"는 문장 자체를 지키는 통제는 아직 없습니다.** §9의 2번이
그것이고, 대상이 두 건뿐이라 지금이 만들기 가장 쉬운 시점입니다.
