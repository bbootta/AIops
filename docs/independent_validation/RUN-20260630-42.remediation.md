# 시정조치 이력 — RUN-20260630-42

절은 **`request_id`로 구성한다.** 회차 숫자를 손으로 세다가 `IVR-68128ECE5694`를
"5차"로 적어 이 문서에 "5차"가 두 번 나타났다 — 지적 F-502. 아래 표가 이 문서의
회차 정본이고, 회차 부여는 **3선이 F-502에서 확정한 열거**를 그대로 따른다
(`RUN-20260630-42.opinion.md` §6.3 · `RUN-20260630-42.approval.md`).

| 회차 | request_id | 이 문서의 절 |
|---:|---|---|
| 1 | `IVR-E6BEA5DA0D5F` | 부적합 시정조치 |
| 2 | `IVR-52D8B21C1A1E` | 지적 시정 |
| 3 | `IVR-573F73DBBF35` | 지적 시정 |
| 4 | `IVR-0DC4055ED5AF` | 검증 결과 및 조건부 결재 |
| 5 | `IVR-AC56730A9613` | 전용 절 없음 — 지적 F-401·F-402에 대한 대응은 `IVR-68128ECE5694` 검증 요청 절에 들어 있다 |
| 6 | `IVR-68128ECE5694` | 검증 요청 · 검증 결과 및 조건부 결재 |
| — | `IVR-35F6EAB36D7F` | 회차 없음 — 요청 패키지는 커밋됐으나(`08cb47a`) 응답 기록이 없고 3선 열거에도 없다. 응답 전에 `IVR-68128ECE5694`로 대체됐다 |

**이 표를 무엇으로 대조하는가.** 요청 이력은
`git log -- docs/independent_validation/RUN-20260630-42.request.json`이 정본이며
request_id 7개가 나온다(위 7행과 일치). **응답 이력으로는 회차를 셀 수 없다** —
`RUN-20260630-42.response.json`은 회차마다 덮어써지고 커밋에 남은 응답은 5건
(1·2·3·4·6차)뿐이다. 5차 응답 파일은 저장소에 없고 그 존재는 3선 진술
(opinion §6.3)에만 남아 있다. 응답 커밋을 세면 `IVR-68128ECE5694`가 5차로 나와
**F-502를 그대로 재생산한다** — 세는 기준을 여기 못 박아 두는 이유다.

본문의 "1차 · 2차 …" 표기는 그 시점의 기록이므로 그대로 둔다. 어느 요청을
가리키는지는 이 표로 찾는다.

---

# IVR-E6BEA5DA0D5F — 부적합 시정조치

- 요청: `RUN-20260630-42.request.json` (리스크관리 팀에이전트)
- 응답: `RUN-20260630-42.response.json` (적합성검증 팀에이전트, 판정 **중부적합**)
- 게이트: `부적합` → 결재 상신 불가 (ISO/IEC 42001 조항 10.1~10.2)

우리 게이트 코드로 응답을 파싱해 동일하게 `부적합`을 확인했고, 지적 6건을
**직접 재현**한 뒤 시정했다. 남의 판정을 그대로 받지 않았고, 반대로 방어하지도
않았다 — 6건 모두 사실이었다.

## 재현 결과

| 지적 | 심각도 | 우리 재현 | 판단 |
|---|---|---|---|
| F-001 자본이 RWA에서 역산 | 중부적합 | seed 42/7/123에서 RWA 8.96~9.97조로 11% 변동해도 `cet1_ratio`가 **0.115000에서 미동 없음** | 사실 |
| F-002 CCR·CVA RWA 누락 | 중부적합 | `rwa_credit_internal = rwa_sa + rwa_irb`, `ccr_result`는 산출만 되고 미합산. 정정 RWA 9,394,620,060,178.572 — 3선 재계산치와 **소수점까지 일치** | 사실 |
| F-003 위기상황 침범이 자체검증에 없음 | 중부적합 | 자체검증 체크 목록에 스트레스 저점 vs 요구치 비교 자체가 없음 | 사실 |
| F-004 레버리지에 파생 미포함 | 경부적합 | `exposure_measure(...)` 호출에 `derivatives` 인자 없음 (기본값 0) | 사실 |
| F-005 Stage 1 커버리지 8.1% | 경부적합 | 합성 데이터 PD 스케일 산물 — 산술 오류는 없음 | 사실 |
| F-006 표준방법 RWA가 내부의 78% | 경부적합 | 내부 9.380조 · 표준 7.328조 = 78.1%, 하한 미구속 확인 | 사실 |

## 시정 내용

**F-001 — 자본 합성을 RWA에서 분리** (`capital/bis.synthesise_capital`)
자본을 총익스포저의 10%(AT1 13%·T2 22% 발행 비중)로 만든다. RWA를 인자로 받지
않으므로 RWA가 커지면 비율이 내려간다. 시정 후 seed 7 → CET1 12.45%,
seed 123 → 10.76%로 **비율이 RWA에 반응**한다.
합성이라는 사실 자체는 `KNOWN_ASSUMPTIONS`에 공시했다.

**F-002 — CCR·CVA를 RWA에 합산** (`pipeline._stage_capital`)
SA-CCR RWA + CVA 소요자본을 신용 RWA에 넣고, 산출하한 분모에도 반영했다.
교차검증 `xd_rwa_components_sum`이 CCR을 빼고 대사하던 것도 함께 고쳐 이 누락이
다시 생기면 FAIL이 나게 했다. 업무보고서 BR-01(BA2101)에 라인 2130을 신설했다.

**F-003 — 자체검증 체크 신설** (`validation.consistency`)
`stress_trough_meets_requirement`: 기준 시나리오 침범은 FAIL, 악화·심각 시나리오
침범은 WARN(자본계획·회복계획 연계 대상)으로 남긴다. 심각 시나리오를 견디는 것은
규제 요구가 아니므로 FAIL로 만들면 거짓 경보가 되기에 등급을 나눴다.
현재 상태: **WARN** — adverse 7.51% · severely_adverse 4.38% (요구 8.00%).

**F-004 — 레버리지 익스포저에 파생 포함**
`exposure_measure(..., derivatives=ccr.ead_total)`. 부외 CCF 10% 일률 적용은
그대로이며 그 사실을 `KNOWN_ASSUMPTIONS`에 공시했다.

**F-005 · F-006 — 공시**
Stage 1 커버리지 스케일과 표준방법/내부모형 역전 구조를 `KNOWN_ASSUMPTIONS`에
추가했다. 3선이 지적한 "공시 기준이 방향에 따라 비대칭"(보수적인 것만 공시)을
받아들여, **과소계상 방향 가정도 함께 공시**하도록 목록을 넓혔다.

## 파급

- 위기상황 엔진에서도 같은 CCR 누락이 있었다 — 시정 과정에서 발견해 함께 고쳤다.
  CCR을 빼면 심도 0에서 기준 상태가 재현되지 않아 추적표 불변식이 깨진다.
- 골든 재고정: `rwa_final_total` 9,380,455,004,957.953 → 9,394,620,060,178.572,
  `cet1_ratio` 0.115 → 0.110779, `total_ratio` 0.155 → 0.149552,
  `leverage_ratio` 0.116013 → 0.111590, `reverse_critical_severity` 0.9447 → 0.8456.
- 자체검증: PASS 49 · WARN 4 · FAIL 0 (WARN +1 = 신설 체크).

## 다음

산출 지문이 바뀌었으므로 **새 요청 ID로 재검증을 요청**한다. 이전 응답은 이전
지문에 대한 것이며, 게이트는 `run_id`·`request_id`가 다르면 승인으로 쓰지 않는다.

---

# IVR-52D8B21C1A1E — 지적 시정

1차 시정 후 재검증에서 **중부적합 1 · 경부적합 2 · 적합 3**을 받았다.
F-002·F-001·F-003은 해소 확인(적합)이고, 새 지적 3건을 다시 시정했다.

## F-101 (중부적합) — 시정이 결함을 옮겼다

3선 지적: 자본을 총익스포저에 비례시키니 레버리지 = (EAD·k)/(EAD·1.01+ccr)로
EAD가 약분되어 이번엔 **레버리지비율이 상수**가 됐다.

우리 반증 실험 (seed 42/7/123/999/2024, EAD 9.76~11.18조 = 14.5% 변동):

| 지표 | 변동폭 | 변동계수 |
|---|---|---|
| CET1 비율 | 415.8bp | 12.048% |
| 레버리지비율 | **1.4bp** | **0.044%** |

지적대로다. 레버리지비율은 위험가중과 무관하게 자본 대비 총익스포저를 제한하는
지표인데, 자본을 익스포저의 상수배로 두면 감지 대상이 소거된다. **결함을 없앤
것이 아니라 CET1에서 레버리지로 옮긴 것**이다.

**시정** — 자본을 파이프라인 **입력**으로 승격했다.

- `run_pipeline(capital_ledger=CapitalStack(...))` — 실제 자본 원장을 주입한다.
- 원장이 없을 때만 합성기를 쓰며, 두 축은 **고정 발행자본**(자본금 5,000억 ·
  AT1 1,400억 · T2 2,400억 — 증자는 이산적 사건이지 자산 증가에 비례하지 않는다)
  + **이익잉여금**(연간이익 × 4년)이다.

> **정정** (3차 지적 F-201) — 이 문단은 원래 이익잉여금을 "수익성의 함수이지
> 익스포저의 함수가 아니다"라고 썼으나 **사실이 아니다**. `data_gen.py:196`이
> `revenue = ead × spread`로 수익을 만들므로 연간이익은 익스포저를 따라간다.
> 규모와 무관한 축은 고정 발행자본뿐이다. 아래 F-201 절 참조.

시정 후 같은 실험:

| 지표 | 변동폭 | 변동계수 |
|---|---|---|
| CET1 비율 | 437.0bp | 13.057% |
| 레버리지비율 | **136.8bp** | **3.741%** |

두 통제가 함께 살아났다.

## F-102 (경부적합) — 변경 영향 공시가 불완전했다

자본 변경이 `alm/balance_sheet.py`의 `equity = capital_total` 경로로 LCR·NSFR을
움직였는데 1차 시정 문서의 '파급' 목록에 두 지표가 빠졌다. 재고정 목록이 실제
변동 항목의 부분집합이면 다음 검토자가 누락분을 정상으로 오인한다 — 지적이 맞다.

**시정** — 파급 목록을 손으로 적지 않는다. 요청서(`*.request.json`)의
`recalc_targets`가 전 headline 값을 담고 있으므로 **그것이 정본**이며, 아래 표는
요청서에서 생성했다.

| 항목 | 키 | 값 |
|---|---|---|
| 위험가중자산 합계 | `rwa_final_total` | 9,394,620,060,179 |
| 보통주자본비율 | `cet1_ratio` | 0.116489 |
| 총자본비율 | `total_ratio` | 0.156938 |
| 레버리지비율 | `leverage_ratio` | 0.117126 |
| 기대신용손실 합계 | `ecl_total` | 94,531,443,665 |
| 유동성커버리지비율 | `lcr` | 1.180709 |
| 순안정자금조달비율 | `nsfr` | 1.130671 |
| 심각 시나리오 CET1 저점 | `stress_trough_cet1` | 0.046912 |
| 역스트레스 임계 심도 | `reverse_critical_severity` | 0.982246 |
| 대손준비금 소요액 | `reserve_shortfall` | 80,848,614,463 |

(요청서 `recalc_targets`에서 생성 — 손으로 옮겨 적지 않는다)

## F-103 (경부적합) — 시정 문서 수치 오기

1차 문서에 `reverse_critical_severity` 0.9447 → 0.8456으로 적었으나 실제 산출은
0.8426이었다(차이 0.0030). 골든은 0.8426으로 재고정해 놓고 문서만 갱신하지
않았다. 산출 오류가 아니라 **문서 오류**이며, 시정 문서는 결재선이 변경 크기를
판단하는 근거이므로 어긋나면 안 된다. F-102 시정(생성된 표)으로 재발을 막는다.

## F-106 권고 반영 — WARN 본문 전달

"신설 체크가 WARN인 이상 `self_validation_failures`는 계속 비어 있으므로 결재선이
WARN 4건의 내용을 실제로 읽도록 본문을 실을 것"을 받아들여, 요청 패키지에
`self_validation_warnings`(체크명 + 상세)를 추가했다. 게이트 원장에도
`n_self_warn`을 남긴다 — FAIL 0이어도 WARN이 규제 미달을 담을 수 있다.

현재 WARN 4건:

- **stress_trough_meets_requirement** — 위기상황 요구치 침범 (자본계획·회복계획 연계 필요) — adverse: 7.94% < 요구 8.00% (2027Q3, 제약 total); severely_adverse: 4.69% < 요구 8.00% (2027Q2, 제약 total)
- **pd_floor_5bp** — 1 exposures below 5bp PD floor (will be floored in IRB)
- **concentration_hhi** — HHI above 0.18 on: country=0.295
- **pd_gini_retail_other** — retail_other Gini 0.220 acceptable but below 양호 기준 0.4

## 3선의 자기정정

F-106에서 3선이 원지적 F-003의 표현("규제 위반")이 부정확했다며 스스로 정정했다.
스트레스 후 자본비율에 현행 최소자본 기준을 그대로 적용하는 것은 규제 요구가
아니라는 우리 등급화(기준 시나리오만 FAIL)를 타당하다고 인정했다. 기록해 둔다 —
독립검증은 일방 판정이 아니라 상호 검증이다.

---

# IVR-573F73DBBF35 — 지적 시정

판정 **경부적합** (중부적합 **0** · 경부적합 3 · 해소 확인 4). 3선이 F-001 ·
F-002 · F-003 · F-101 네 건의 중부적합이 모두 해소됐고 독립 재계산 10/10이
일치함을 확인했다. 남은 3건은 **산출값 오류가 아니라 문서 · 구조 · 도구**에
관한 것이다. 세 건 모두 코드로 재현해 사실임을 확인했다.

## F-207 (경부적합 · 선결) — 게이트 코드가 스스로 모순됐다

3선이 이번에 **판정 도구 자체**를 검증 대상에 넣었다. `ValidationResponse.passes`의
docstring은 "경부적합까지는 조건부 통과"라고 약속했으나 구현은
`verdict == "적합"`만 통과시켜, 약속한 조건부 경로가 **코드에 존재하지 않았다**.
`ValidationResponse(verdict="경부적합", findings=[]) → passes = False`.

우리 게이트로 3차 응답을 파싱한 결과가 정확히 그 상태다.

```
status : 부적합        reason : 판정 경부적합
중부적합 : (없음)      경부적합 : F-201 · F-202 · F-207
```

지적이 맞다. 판정 도구가 문서와 어긋나면 그 위의 모든 판정이 흔들린다.

**시정** — 세 번째 상태 `조건부`를 도입했다. docstring만 고쳐 "적합만 통과"로
맞추는 선택지도 있었으나, 그러면 문서 지적 수준의 경부적합 하나가 결재를 영구히
막아 게이트를 우회할 유인이 생긴다. 반대로 자동 통과시키면 사람의 판단이 사라진다.

| 상태 | 조건 | 결재 |
|---|---|---|
| `응답대기` | 응답 파일 없음 | 불가 |
| `부적합` | 중부적합 있음 · 재계산 불일치 · run_id/request_id 불일치 | 불가 |
| **`조건부`** | 경부적합 + 중부적합 0건 + 재계산 전건 일치 | **기록 있을 때만** |
| `적합` | 판정 적합 + 재계산 전건 일치 | 가능 |

`조건부`는 `require(ConditionalApproval(...))`로만 통과하며, 기록은
**잔여위험 · 후속조건 · 이행기한 · 배포 범위 · 결재자**를 모두 요구한다. 하나라도
비면 통과하지 않는다 — fail-closed는 유지된다. 기계가 이 기록을 대신 만들지
않는다.

재계산 불일치 검사를 판정 검사보다 **앞으로** 옮겼다. 어긋난 재계산은 어떤
판정과도 무관하게 부적합이어야 한다.

회귀 방지 테스트 7건을 추가했다 — 그중 `test_passes_docstring_matches_the_implementation`은
docstring이 약속한 경로가 실제로 존재하는지를 직접 검사한다.

## F-201 (경부적합) — 시정 문서의 사실 주장이 코드와 어긋났다

2차 시정 문서에 "이익잉여금은 수익성의 함수이지 익스포저의 함수가 아니다"라고
썼다. **틀렸다.** `data_gen.py:196`이 `revenue = ead × spread`이므로 연간이익은
익스포저의 함수다. 3선 실측대로 이익/EAD가 seed 5종에서 0.013433~0.014853
(변동계수 3.777%)로 좁게 유지되며, CET1 1.0944조 중 유보분이 **54.3%**다.
우리 자체검증으로 재현한 값도 54.3%로 일치한다.

F-103과 같은 유형의 문서–산출 불일치다.

**시정** — 세 곳의 주장을 사실에 맞게 고쳤다.

- `capital/bis.py` 주석 · `synthesise_capital` docstring — 규모 독립성은 고정
  발행자본에서만 나오며 이익잉여금은 익스포저를 따라간다고 명시.
- `KNOWN_ASSUMPTIONS` — 기존 항목의 "익스포저에도 비례하지 않으므로"를 삭제하고,
  규모 비례분이 CET1의 54%라는 사실을 별도 가정으로 신설해 3선에 공시.
- 2차 절의 해당 문단에 정정 표기.

## F-202 (경부적합) — 자산이 커지면 레버리지 반응성이 다시 소멸한다

`leverage = (고정 6,400억 + 4×margin×EAD) / (1.01×EAD + ccr_ead)` 이므로
EAD → ∞ 에서 `4×margin/1.01`로 수렴한다는 지적을 자산 배수 실험으로 확인했다.

| 자산 배수 | EAD | CET1 비율 | 레버리지비율 |
|---:|---:|---:|---:|
| ×1 | 10.4조 | 0.116489 | **0.117126** |
| ×2 | 20.8조 | 0.089829 | 0.086762 |
| ×5 | 52.0조 | 0.073847 | 0.068544 |
| ×10 | 104조 | 0.068522 | 0.062471 |
| ×50 | 520조 | 0.064262 | **0.057613** |

×50에서 고정 발행자본은 총익스포저의 0.12%에 불과해 지적대로 반응성이 소멸한다.
현재 seed 42 기준 136.8bp의 반응성은 고정분 기여 52% 덕분이며, 자산이 몇 배만
커져도 유지되지 않는다.

**시정** — 두 가지를 했다. 합성 데이터에는 실제 자본 원장이 없으므로 원장을
기본 경로로 삼을 수는 없다. 대신,

1. **정책 명시** — 고정 발행자본은 자산 규모와 무관하게 유지한다는 규칙을
   `bis.py`에 못 박았다. 자산이 늘었다고 이 상수를 함께 키우면 자본이 다시
   규모의 함수가 되어 통제가 소멸한다. 수렴 실측치도 함께 남겼다.
2. **상시 감시 체크 신설** — `capital_source`(자체검증). 실제 원장을 주입하면
   PASS, 합성기를 쓰면 규모 비례분 비중을 계산해 50% 이상이면 **WARN**이다.

```
WARN | 합성 자본 사용 — CET1 1,094,368,959,484 중 규모 비례분(이익잉여금) 54.3%,
       고정 발행자본 기여 51.8% · 자산 대비 고정분 6.15%
       (희석될수록 레버리지 반응성이 소멸)
```

이 WARN은 `self_validation_warnings`를 통해 매 요청마다 3선과 결재선에
전달된다. 지표가 상수로 수렴하는 과정은 조용히 진행되므로, 비중을 매번 눈에
보이게 두는 것이 F-201·F-202의 공통 뿌리에 대한 상시 통제다.

## 3선 권고 반영

- **권고 4** (2·3차 연속 제기, "이 지표가 상수가 되는 입력 조합이 있는가"를
  자체검증 체크로) — 위 `capital_source`가 그 첫 구현이다. 자본이 규모의 함수가
  되는 정도를 매 실행 정량화한다.
- **권고 5** (`reverse_critical_severity`의 해석 폭) — 1차 0.9447 → 2차 0.8426 →
  3차 0.9822(진폭 0.14)로 자본 가정에 민감하다는 사실을 `KNOWN_ASSUMPTIONS`에
  넣어 결재선과 3선에 함께 전달한다.

## 자체검증 변화

WARN 4건 → **5건** (신설 `capital_source`). FAIL은 0건 유지.

## 시정 중 우리가 찾은 결함 — 요청 식별자가 내용 변경을 못 잡았다

3차 시정을 마치고 4차 요청을 만들었더니 `request_id`가 **IVR-573F73DBBF35 그대로**
나왔다. 공시 가정이 12건 → 15건, 자체검증 WARN이 4건 → 5건으로 바뀌었는데도
식별자가 같아, 그 변경을 본 적 없는 **3차 응답이 그대로 승인으로 통했다**
(게이트가 `조건부`를 반환했다).

원인은 `request_id = digest(run_id, headline_digest)` — headline **수치만**
지문화한 것이다. 이번 시정처럼 산출값이 하나도 안 바뀌는 시정(문서·통제·가정
공시)일수록 재검증이 필요한데, 게이트의 stale 방어가 정확히 그때 뚫린다.
F-207과 같은 종류의 결함이다 — 판정 도구 자체의 구멍이며, 3선이 게이트를 검증
대상에 넣지 않았다면 우리도 보지 못했을 것이다.

**시정** — 지문에 **3선에게 넘기는 것 전부**를 넣었다: `known_assumptions` ·
자체검증 요약 · FAIL 목록 · WARN 본문. 4차 요청은 `IVR-0DC4055ED5AF`로 바뀌었고
게이트는 3차 응답을 `부적합 — request_id 불일치`로 거절한다. 회귀 테스트
`test_request_id_changes_when_the_disclosed_content_changes`를 추가했다
(headline_digest는 그대로인데 식별자만 바뀌는지 확인).

## 다음

게이트 규약·공시 가정·자체검증 항목이 바뀌었으므로 새 요청
**IVR-0DC4055ED5AF**로 4차 검증을 의뢰한다. 3차 응답은 이 요청의 승인으로 쓰지
않는다.


---

# IVR-0DC4055ED5AF — 검증 결과 및 조건부 결재

판정 **경부적합** (중부적합 **0** · 경부적합 1 · 해소 확인 5). 독립 재계산
**10/10 일치**, 산출값 오류 0건. 게이트 상태 **`조건부`**.

## F-301 (경부적합) — 지문의 stale 방어가 아직 완전하지 않다

3차 시정에서 request_id 지문에 `run_id · headline_digest · KNOWN_ASSUMPTIONS ·
자체검증 요약 · FAIL · WARN` 여섯 항목을 넣었으나, `recalc_targets`의
`citation`·`korean`이 빠졌다. 규정 근거가 바뀌면 3선은 같은 수치를 다른 기준으로
검증해야 하므로 재검증 대상인데, 이전 응답이 그대로 승인으로 통한다.

**우리 재현** — `cet1_ratio`의 citation을 `은행업감독규정 제26조` → `전혀 다른
규정 XYZ-999`로 바꿔 요청을 재생성했으나 `request_id`가 `IVR-0DC4055ED5AF`로
**불변**이었다. 지적대로다.

3선의 원인 분석이 정확하다 — 지문 대상을 **필드 단위로 열거한 설계**가 문제이며,
F-102에서 세운 "파급 목록을 손으로 적지 않는다"는 원칙을 지문 설계에는 적용하지
않았다. 저희가 3차에 고친 결함과 같은 종류이고 범위만 좁아진 것이다.

## 조건부 결재

결재 책임자가 F-301을 잔여위험으로 인수하고 조건부 결재했다
(`RUN-20260630-42.conditional_approval.json` · `.approval.md`).

| 항목 | 내용 |
|---|---|
| 인수한 잔여위험 | F-301 — 규정 인용 변경 시 이전 응답이 승인으로 재사용될 수 있음 |
| 후속조건 1 | `request_id` 지문을 요청 객체 전체 직렬화로 전환 (`asdict`에서 `request_id`·`created_at`만 제외) |
| 후속조건 2 | 회귀 테스트를 "어떤 필드를 바꿔도 ID가 변한다"로 일반화 |
| 이행기한 | 2026-08-09 |
| 배포 범위 | `RUN-20260630-42` (asof 2026-06-30 · seed 42) **단일 실행 한정** |
| 강화 모니터링 | 다음 요청 시 3선이 지문 완전성 반증 재수행 |

## 게이트 통과 — 2선 코드로 재현

```
요청        IVR-0DC4055ED5AF
응답        IVR-0DC4055ED5AF | 판정 경부적합 | 재계산 10/10 일치
게이트 상태  조건부
기록 없이 require()      → 차단
결재 기록으로 require()  → 통과 · 결재 상신 가능
```

F-207 시정으로 만든 `조건부` 경로가 처음 실제로 쓰였고, **기록 없이는 막히고
기록이 있어야 열린다**는 설계대로 동작했다.

## 후속조건 이행 — 기한 2026-08-09, **즉시 이행 완료**

**조건 1 — 지문을 요청 객체 전체 직렬화로 전환**

`request_identifier(request)`를 신설했다. `asdict(request)`에서 `request_id`와
`created_at`만 제외하고 canonical JSON(`sort_keys` · 구분자 고정)으로 해시한다.
제외 사유는 각각 "지금 계산 중"과 "벽시계라 내용이 같아도 매번 달라짐"이며,
그 외에는 아무것도 빼지 않는다. 필드가 늘어도 자동으로 덮이므로 `citation` ·
`branch` · `artefacts` 누락이 함께 닫혔다.

핵심은 코드가 아니라 설계 원칙이다. 지문 대상을 **손으로 열거하면** 열거에서
빠진 것이 조용히 stale 방어를 뚫는다 — 이번까지 두 번 그랬다.

| 시점 | 지문 대상 | 뚫린 것 |
|---|---|---|
| ~3차 | headline 수치만 | 공시 가정 · 자체검증 WARN |
| 3차 시정 | 여섯 항목 열거 | `recalc_targets`의 `citation` (F-301) |
| **4차 시정** | **요청 전체 (2개 필드만 제외)** | — |

F-102에서 "파급 목록을 손으로 적지 않는다"는 원칙을 세워 놓고 정작 지문 설계에
적용하지 않은 것이 원인이라는 3선 분석이 정확했다.

**검증** — `cet1_ratio`의 citation을 `은행업감독규정 제26조` → `전혀 다른 규정
XYZ-999`로 바꾸니 `IVR-AC56730A9613` → `IVR-B79EEC2C669C`로 변동한다. F-301의
반증 실험이 더는 재현되지 않는다.

**조건 2 — 회귀 테스트를 전수 명제로 일반화**

`test_any_field_change_moves_the_request_id` — `ValidationRequest`의 필드를
`dataclasses.fields()`로 열거해 **제외 2건을 뺀 전부**를 하나씩 변형하고, 각각
식별자가 변하는지 확인한다. "특정 필드를 바꾸면 변한다"가 아니라 "어떤 필드를
바꿔도 변한다"이므로, 새 필드가 늘어도 테스트가 자동으로 덮는다. 제외 목록이
넓어지는 것도 막았다(`test_the_id_ignores_only_itself_and_the_wall_clock`).

## 남은 일

조건 이행으로 지문이 `IVR-0DC4055ED5AF` → **`IVR-AC56730A9613`**으로 바뀌었다.
게이트는 4차 응답을 `부적합 — request_id 불일치`로 거절한다. 본 승인은
`RUN-20260630-42` 단일 실행 한정이므로 후속 실행에는 어차피 재검증이 요구되며,
새 요청 **`IVR-AC56730A9613`**(5차)으로 재검증을 의뢰한다.


---

# IVR-68128ECE5694 — 검증 요청

4차 조건부 결재(F-301 후속조건)를 이행한 뒤, 금감원 업무보고서를 **제출대상
282건 전건**으로 확대했다. 산출 지문이 바뀌었으므로 새 요청으로 재검증을 의뢰한다.
4차 조건부 결재는 `RUN-20260630-42` 단일 실행 한정이라 본 산출물에는 적용되지 않는다.
직전 요청 `IVR-AC56730A9613`에 대한 3선 응답은 경부적합(F-401 · F-402)이었고,
아래 서식 확대가 그중 F-402에 대한 대응이다.

## 무엇이 바뀌었나

**서식번호 정정 (선결)** — 내부 코드 16건이 실재 FINES 코드와 한 글자 차이로
전혀 다른 서식을 가리키고 있었다. `BA` 접두어는 실재하며 바젤Ⅲ 신설 8건 전용인데
그 이름공간을 점유하고 있었다. 금감원 FINES 마스터를 확보해 기계 생성으로 전부
정정했다 (`fss_master.py` · `tools/gen_fss_master.py`).

**서식 34건 → 290건** — 리스크 소관 117건 + 그 외 165건 + 내부 관리보고 8건.
제출 대상이 아닌 17건(외은 국내지점·특수은행 근거법 전용)은 사유와 함께 분리해
누락과 구분했다.

| 요청 | 서식 | 라인 | 서식검증 |
|---|---:|---:|---:|
| `IVR-0DC4055ED5AF` | 34 | 508 | 176 |
| `IVR-68128ECE5694` | **290** | **5,842** | **1,732** (실패 0) |

> **정정** (6차 지적 F-501) — 이 표의 라인 수와 아래 "실측 68.2%"는
> `asof=2026-06-11`(시험 고정일) 실행의 값이고, 제출물은 `asof=2026-06-30`이다.
> **문서가 제출물과 다른 실행을 설명하고 있었다.** 제출물의 정본 통계는
> `IVR-68128ECE5694 — 검증 결과 및 조건부 결재` 절의 생성 구간에 있다.

**CVA 기준 정정** — `ccr.py`는 반환값을 소요자기자본(K_BA)이라 문서화하는데
`pipeline.py`는 RWA로 그대로 합산했고, 주석은 "이미 RWA 환산치"라고 반대로 적혀
있었다. MAR50.2·RBC20.6에 따라 12.5배 환산한다. CVA RWA 4.6억 → 57.5억,
총 RWA +0.056%.

**산출 근거 분류 신설** (`risk_lib.regulatory.provenance`) — 라인마다 실측·파생·
혼합·대용·미산출·미영위를 판정하고, 원장 26종별로 무엇이 해소되는지 표로 낸다.

## 3선이 특히 도전해야 할 지점

1. **파생 비중** — 5,842라인 중 실측은 68.2%다. 나머지는 원장이 없어 파생했다.
   합계는 산출값에 앵커했으므로 명세 대사가 자기충족은 아니지만 **개별 배분은
   실측이 아니다**. 파생이 실측처럼 보이는 라인이 있는지 봐 달라.
2. **검증의 독립성** — 신용카드 그룹 적대적 검토에서 *가정을 목표값에 역설계한
   뒤 그 목표값을 FormCheck로 확인하는* 구조가 나왔다(B2824 수수료율). 같은
   패턴이 다른 서식에 남아 있는지 봐 달라 — 이런 검증은 통과해도 의미가 없다.
3. **규정 인용** — 자체 검토에서만 규정 오인용이 여러 건 나왔다(자본 정의를
   CRE40, BA-CVA를 SA-CVA 조항, 여전법 감독규정 제11조를 자산건전성 분류로).
   확인되지 않는 문단번호는 장 단위로 낮췄으나 남아 있을 수 있다.
4. **B2824 혼합기준** — 가맹점수수료율이 연간수익÷당월매출이라 12배 크다.
   구조적 한계로 해소하지 못하고 공시만 했다. 판단을 구한다.

## 상태

```
자체검증 (2선)      PASS 49 · WARN 5 · FAIL 0
상시 독립검증 (3선)  응답대기 (IVR-68128ECE5694)
도전 대상 가정        22건
```


---

# IVR-68128ECE5694 — 검증 결과 및 조건부 결재

판정 **경부적합** (중부적합 **0** · 경부적합 2 · 해소 확인 4). 독립 재계산
**10/10 일치**, 산출값 오류 0건. 게이트 상태 **`조건부`**.

- 응답: `RUN-20260630-42.response.json` · 의견서: `RUN-20260630-42.opinion.md`
- 결재: `RUN-20260630-42.approval.md` · `RUN-20260630-42.conditional_approval.json`

해소 확인은 CVA 단위 정정(F-503) · 서식 확대(F-504, 5차 지적 F-402 해소) · 산출
근거 분류 신설(F-505, 미분류 0건 · 미귀속 0건) · 독립 재계산 10종 전건 일치
(F-506) 넷이다. 남은 경부적합 2건은 둘 다 **문서 기재**에 관한 것이며 산출값에는
영향이 없다.

## F-501 (경부적합) — 문서가 제출물과 다른 실행을 설명하고 있었다

3선 지적: 요청 패키지와 시정 문서가 "5,842라인 중 실측 68.2%"라고 적었으나 독립
재현은 5,894라인 · 실측 4,000건(67.87%)이다. 같은 실행에서 headline 10종이 비트
단위로 일치하므로 산출 조건 차이가 아니라는 3선 판단도 맞다.

**사실이다.** 재현해 보니 두 값은 **기준일이 다른 두 실행**에서 나온 것이다.

| 실행 | 서식 | 라인 | 실측 | 실측 비중 |
|---|---:|---:|---:|---:|
| `asof=2026-06-11` (시험 고정일) | 290 | 5,842 | 3,987 | 68.2472% |
| `asof=2026-06-30` (**제출물**) | 290 | 5,894 | 4,000 | 67.8656% |

문서에 적은 값은 시험 고정일 실행이고 제출물은 기준일 실행이다. 즉 **문서가
제출물과 다른 실행을 설명하고 있었다.** 임시 스크립트의 출력을 눈으로 읽어 시정
문서와 `KNOWN_ASSUMPTIONS`에 옮겨 적은 것이 원인이다. 산출값 오류는 아니지만,
문서를 2차 인용하는 검토자·결재선은 제출물과 다른 근거로 판단하게 된다.

(위 표는 원인 규명을 위한 1회성 대조 기록이다 — 제출물의 정본 통계로 인용하지
않는다. 정본은 바로 아래 생성 구간이 만든다.)

이 유형은 F-103(문서 수치 오기) · F-201(문서 주장이 코드와 불일치) ·
F-401(가정이 코드보다 낡음)에 이어 **네 번째 반복**이다. 네 번 모두 틀린 수치를
고치고 "다음엔 대조하겠다"로 끝냈고, 네 번 모두 다시 났다. 고칠 것은 값이 아니라
**값이 문서에 손으로 들어오는 경로**다.

### 제출물 산출 근거 — 코드가 만드는 정본

<!-- generated: provenance -->
기준일 2026-06-30 · 서식 290건 — risk_lib.regulatory.provenance 생성 (손으로 적지 않는다)

| 산출 근거 | 라인 수 | 비중 |
|---|---:|---:|
| 실측 | 4,002 | 67.88% |
| 파생 | 1,107 | 18.78% |
| 미산출 | 248 | 4.21% |
| 서술 | 186 | 3.15% |
| 미영위 | 172 | 2.92% |
| 혼합 | 167 | 2.83% |
| 대용 | 14 | 0.24% |
| **합계** | **5,896** | **100.00%** |
<!-- /generated -->

요청서가 3선에 넘기는 문장도 같은 정본에서 나온다. F-501이 실제로 틀렸던 것은
표가 아니라 이 산문 형태였다.

<!-- generated: provenance_sentence -->
산출 근거 분류(실행 시점 생성): 산출한 서식 290건 · 전체 5,896라인 기준 실측 67.9% · 파생 18.8% · 미산출 4.2% · 서술 3.2% · 미영위 2.9% · 혼합 2.8% · 대용 0.2%. 이 수치는 문서에 손으로 적지 않고 provenance_stats가 만든다 — 라인별 근거와 원장별 해소 경로는 risk_lib.regulatory.provenance가 산출한다 (산출물 Pack 05_regulatory/산출근거_라인별.csv · 지적 F-501).
<!-- /generated -->

## F-502 (경부적합) — 회차 표기가 어긋났다

**사실이다.** 본 요청 `IVR-68128ECE5694`는 6차인데 시정 문서와 커밋 제목이
"5차"로 적었다. 직전 `IVR-AC56730A9613`이 5차이므로 이 문서에 "5차"가 두 번
나타나게 됐고, 회차로 절을 구성한 문서에서는 어느 지적에 대한 대응인지 흐려진다.

게이트 판정에는 영향이 없다 — 요청서에 회차 필드가 없기 때문이다. 바꿔 말하면
회차는 **아무 코드도 검사하지 않는 손으로 적은 값**이었고, 그래서 틀렸다.

**시정** — 절 제목에서 회차 숫자를 빼고 `request_id`를 제목으로 삼았다. 회차는
문서 머리말의 이력표에서만 나오며, 그 표의 회차 부여는 3선이 F-502에서 확정한
열거를 따르고 요청 이력(`request.json`의 커밋 이력)으로 대조한다. 응답 이력으로
세면 5차가 나와 이 지적을 되풀이한다는 점도 머리말에 적어 두었다. 본문의
"1차 · 2차 …"는 그 시점 기록이므로 남겼고, 어느 요청을 가리키는지는 이력표로
찾는다.

## F-503 (해소 확인) — 3선의 자기정정: 합계 대사는 단위 오류를 잡지 못한다

CVA 단위 정정(K_BA × 12.5)이 확인됐는데, 3선이 **그 오류의 출처가 자신의
지적이었음**을 함께 기록했다.

| 시점 | 3선 판정 | 실제 |
|---|---|---|
| 1차 F-002 | "CVA 소요자본을 RWA 합계에 넣으라" | 12.5배 환산이 필요했다 |
| 2차 F-104 | "합산됐고 차이 0 → **적합**" | 차원 오류를 보지 못했다 |
| 6차 F-503 | 2선이 자체 검토로 발견·정정 | K_BA × 12.5 |

소요자본과 RWA는 12.5배 차원이 다른데 3선의 독립 재계산이 합계 일치만 확인했다.
3선은 이를 판정 오류로 인정하고 프로토콜에 `ADV-CALC-04`(단위·차원 정합성,
severity `critical`)를 신설했다.

기록해 두는 이유는 F-106 때와 같다 — **독립검증은 일방 판정이 아니다.** 3선
지적이라고 그대로 수용했다면 이 차원 오류는 남았을 것이고, 3선이 스스로 정정하지
않았다면 판정 도구의 구멍이 그대로 있었을 것이다. 양쪽 다 필요하다.

## 조건부 결재

결재 책임자가 F-501 · F-502를 잔여위험으로 인수하고 조건부 결재했다.

| 항목 | 내용 |
|---|---|
| 인수한 잔여위험 | F-501 문서 기재 통계가 코드 사실과 불일치(네 번째 반복) · F-502 회차 표기 불일치 |
| 후속조건 1 | 기계로 산출 가능한 통계를 문서에 손으로 적지 말고 `provenance_frame`에서 **생성**하도록 전환 (모형개발) |
| 후속조건 2 | 요청서 가정·시정 문서의 수치를 코드 사실과 대조하는 **자체검증 체크** 신설 (모형개발) |
| 후속조건 3 | 회차 표기를 `request_id` 정본에서 파생하거나 표기하지 않음 (업무소유자) |
| 이행기한 | **2026-08-10** |
| 배포 범위 | `RUN-20260630-42` (asof 2026-06-30 · seed 42) **단일 실행 한정** |
| 승인 대상 제외 | **문서에 손으로 적힌 산출 근거 통계(라인 수 · 실측 비중 · 회차)** — 코드 산출값만 결재 범위다 |
| 강화 모니터링 | 다음 요청 시 3선이 문서 기재 통계와 코드 산출값을 전수 대조 · `ADV-CALC-04`를 모든 합산 항목에 적용해 합계 일치만으로 적합 판정하지 않음 |

'승인 대상 제외' 줄이 이번 결재의 성격을 정한다. 결재 책임자는 산출값을 승인하면서
**문서에 손으로 적힌 통계는 승인하지 않았다.** 그 통계가 승인 범위 안으로
들어오려면 사람이 적기를 그만두는 수밖에 없다.

## 후속조건 이행

| # | 상태 |
|---|---|
| 1 · 2 | 코드로 이행 중 — 통계 생성과 문서–코드 대조 자체검증 체크. 위 `provenance` 생성 구간이 그 대조 대상이다 |
| 3 | **이행** — 절 제목을 `request_id` 중심으로 바꾸고 회차를 머리말 이력표에서 파생하게 했다 (F-502 절) |

조건 3은 표기 규칙이라 코드가 강제하지 않는다. 회차를 셀 자리를 이력표 한 곳으로
줄이고 대조 기준(요청 이력)을 못 박은 것이 전부이며, **이력표 자체가 낡는 것은
막지 못한다.** 조건 1·2가 닫는 것은 수치이지 회차가 아니다. 기계로 닫으려면
`request.json`의 request_id 이력을 열거해 이력표와 대조하는 체크가 필요하다 —
다만 응답 파일이 회차마다 덮어써지므로 이력의 원천은 커밋 이력뿐이고, 그 대조를
코드로 만들려면 회차별 요청·응답을 파일로 남기는 것이 선행돼야 한다.

## 다음

의견서 §9의 이월 3건은 본 결재로 해소되지 않았다 — 자기충족 FormCheck 패턴
전수 점검(각 FormCheck 기대값의 `expected_source` 표시 요청) · 규정 오인용 목록
제출 · B2824 혼합기준 판단(현 산출물에 해당 서식이 없어 확인 불가). 다음 요청
패키지에 실어 넘긴다.

## 상태

```
자체검증 (2선)      후속조건 1·2로 체크가 늘어나는 중 — 확정 집계는 요청 패키지가 정본
상시 독립검증 (3선)  경부적합 · 게이트 조건부 (IVR-68128ECE5694) — 조건부 결재 기록으로 통과
후속조건            3건 중 1건 이행 (조건 3) · 이행기한 2026-08-10
```

(자체검증 집계를 숫자로 적지 않는 것은 F-501과 같은 이유다. 지금 이 문서를 쓰는
시점에 조건 1·2로 체크가 늘고 있어, 손으로 적으면 적는 순간 낡는다.)


---

# 7차 검증 요청 — `IVR-2BFA6A52C664`

6차 조건부 결재의 후속조건 3건을 이행기한(2026-08-10) 전에 처리했다. 지문이
이동했으므로 6차 조건부 결재는 본 산출물에 적용되지 않으며, 게이트가 6차 응답을
`request_id` 불일치로 거절한다 — 설계대로다.

## 후속조건 이행

**조건 1 — 통계를 생성한다.** `KNOWN_ASSUMPTIONS`에서 박힌 숫자를 걷어내고
`ValidationRequest.provenance` 구조화 필드를 신설했다. 3선은 문장을 파싱하지 않고
바로 대조할 수 있다. 레지스트리 건수(제출대상·리스크 소관)도 `fss_master`에서
세도록 바꿨다 — 손으로 적힌 값은 마스터가 한 건만 늘어도 조용히 낡는다.

**조건 2 — 문서↔코드 대조를 기계에 맡긴다.** `risk_lib.validation.doc_figures`가
문서의 `<!-- generated: 이름 -->` 구간만 정본과 대조한다. 회차별 기록이 누적된
문서에서 전부 대조하면 거짓 경보가 쏟아지고, 그러면 다음 사람이 검사를 끈다.
조립 시점(`build_studio`)에 결선했고 결과는 자체검증 집계에 합산된다.

**조건 3 — 회차 표기를 `request_id` 기준으로 전환했다.**

## 통제가 실제로 작동하는지

변이시험으로 확인했다. 제출 기준일 실행에서 문서를 낡게 만들면 자체검증이
`FAIL 1`(`doc_figures_provenance`)을 싣고 게이트가 막는다. 비중을 F-501 원형인
68.2%로 되돌려도, 기준일을 바꿔도, 구간을 지우거나 표시자를 깨뜨려도 FAIL이다.
반대로 이 실행의 문서가 아니면 대조 대상이 0건이라 거짓 경보가 없다.

결선 과정에서 내가 만든 결함 두 건도 전체 스위트가 잡아냈다 — (1) `build_studio`가
`result.validation`을 변형해 파이프라인 자체검증 집계가 호출 순서에 의존했고,
(2) 특정 실행을 설명하는 문서를 모든 실행에 들이대 시험 고정일에서 항상 FAIL이
났다. 둘 다 좁은 변이시험으로는 안 잡혔고 전체 스위트가 처음 드러냈다.

## 3선이 도전해야 할 지점

1. **통제의 사각** — 생성 구간 **밖**의 수치는 여전히 손으로 적힌다. 과거 회차
   기록은 그 시점 사실이므로 대조 대상이 아니지만, 새로 쓰는 산문에 수치를
   적으면 구간 밖이라 잡히지 않는다. 이 사각이 실제로 얼마나 큰지 봐 달라.
2. **잔여 하드코딩** — `KNOWN_ASSUMPTIONS`에 손으로 적힌 건수가 남아 있다
   (내부 관리보고 · 외은 국내지점 · 정정한 내부 코드). 가드를 건수까지 넓히면
   정당한 표기에 거짓 경보가 대량 발생해 막지 않았다. 판단을 구한다.
3. **파생 비중** — 합계는 산출값에 앵커했으나 개별 배분은 실측이 아니다.
4. **B2824 혼합기준** — 6차에서 해소하지 못하고 공시만 한 상태 그대로다.

## 상태

```
자체검증 (2선)      PASS 51 · WARN 5 · FAIL 0
상시 독립검증 (3선)  응답대기 (`IVR-2BFA6A52C664`)
도전 대상 가정        23건
```


---

# `IVR-4F87EE098FF3` — 검증 요청

`IVR-2BFA6A52C664` 지적 3건(F-601 중부적합 · F-602 · F-603)을 시정했다.

## F-601 — 대손준비금 적용 단위

지적대로다. 3선 재계산치와 소수점까지 일치하게 재현했다.

| | 금액 |
|---|---:|
| 최저적립액 합계 | 128,113,874,008.83 |
| IFRS 9 충당금 합계 | 94,531,443,664.95 |
| **합계 기준 차액 (규정 금액)** | **33,582,430,343.88** |
| 건별 미달액 합산 (보고값) | 80,848,614,462.94 |
| 과대분 | 47,266,184,119.06 (2.4075배) |

과대분이 **건별 초과적립분과 정확히 일치**한다 — 정상 여신에서 ECL이 최저적립액을
넘는 부분이 상계되지 못하고 버려진 것이다. Σmax(0,xᵢ) ≥ max(0,Σxᵢ)가 정리이므로
이 오류는 항상 과대 방향으로만 난다.

`reserve_requirement()`를 신설해 규정 금액을 한 곳에서 산출한다. BR-11은 규정
금액을 싣고 건별 합산은 참고로 내렸으며 **상계 효과를 별도 라인**으로 드러냈다.

### 왜 8개 요청 내내 살아남았나

시정하며 층을 세어 보니 넷이었다.

| 층 | 상태 |
|---|---|
| 1선 산출 | 익스포저 단위 합산 (틀린 적용 단위) |
| 2선 자체검증 | 구조상 실패 불가능한 항등식이 지킴 (F-602) |
| 2선 테스트 | **틀린 해석을 의도로 못박음** — 시정 중 발견 |
| 3선 독립검증 | 같은 산식 재계산 → 6회 연속 "일치" (3선 자기정정) |

테스트 이름이 `test_reserve_shortfall_is_exposure_level_not_netted`였고 docstring에
"총액에서 상계하면 과소산정된다"고 적혀 있었다. 대손준비금은 개별 여신의 충당금이
아니라 **총 충당금을 규제 최저수준까지 끌어올리는 이익잉여금 처분**이므로 본래
합계 개념인데, 그 해석을 테스트가 고정하고 있었다.

**네 층이 같은 방향으로 틀렸고 그중 셋은 우리가 만들었다.** 재계산 일치가
정합성을 보증하지 않는다.

## F-602 — 통제력 0인 검증, 그리고 그 범위

지적대로 `min(0, 건별합산 − 합계기준)`은 두 값의 대소가 정리로 정해져 있어 어떤
자료에서도 정확히 0이었다. 3선이 범위를 BR-11 1건으로 한정하고 나머지를
"미확인"으로 남겼으므로, **그 질문에 먼저 답했다.**

seed 3종(42·7·2024) 교차 측정 결과 동일 유형을 더 찾았다.

```
min(0, (watch + npl) − npl)   → npl 소거 후 min(0, watch), watch ≥ 0 이므로 항상 0
min(0, path.max() − mean)     → 최대 ≥ 평균은 정리이므로 항상 0
```

BR-26의 자기소거 검증을 실효 있는 대사 3건으로 교체했다. 측정치는
`check_strength()`가 매 실행 세어 가정 목록에 **생성**해 싣는다 — 손으로 적으면
F-501·F-603이 세 번째로 반복된다.

**판별은 미완이다.** 양변 0 검증 중 다수는 정당하다(신탁·종금·투자자문 미영위,
유동화 미보유 — 원장이 생기면 살아난다). 그러나 구조적 항진명제도 같은 모습이며,
경험적 구분(seed 교차)이 내가 할 수 있는 한계다.

## F-603 — 생성 구간 밖 사각

지적대로 가정 19의 회차별 계열이 CVA 12.5배 정정 때 이동한 값에서 멈춰 낡아
있었다. 손으로 적은 계열을 걷어내고 성질만 남겼으며, 값은 `recalc_targets`의 git
이력이 정본임을 명시했다. 가정 9·10이 "고정 발행자본"을 서로 다른 범위로 쓰던 것도
갈랐다.

## 통제가 실제로 작동한 기록

이번 회차에 **문서 대조가 나를 잡았다.** BR-26에 라인을 추가해 실측 라인이
4,000 → 4,002로 바뀌었는데 문서를 갱신하지 않았고, 요청서 생성 시 자체검증이
`FAIL 2`를 냈다.

```
FAIL doc_figures_provenance          문서 「| 실측 | 4,000 | 67.87% |」
                                     코드 「| 실측 | 4,002 | 67.88% |」
FAIL doc_figures_provenance_sentence
```

F-501이 재발할 경로가 그대로였고 기계가 막았다. 6차 후속조건 2가 요구한 상태다.

## 3선이 도전해야 할 지점

1. **양변 0 검증 중 어느 것이 항진명제인가** — 기호적 판별은 3선 몫으로 넘긴다.
2. **headline 10종의 적용 단위** — `reserve_shortfall`이 적용 단위 때문에 틀렸다면
   `ecl_total`·`rwa_final_total`도 같은 질문을 받아야 한다. 3선이 신설한
   `ADV-CALC-05`가 겨냥하는 지점이다.
3. **가정 24건 중 생성은 2건** — 나머지는 손으로 적은 산문이고 낡을 수 있다.
   이번에 그중 하나(가정 19)가 실제로 낡은 채 발견됐다.
4. **파생 비중 · B2824 혼합기준** — 이전 회차에서 이월, 해소되지 않았다.

## 상태

```
자체검증 (2선)      PASS 51 · WARN 5 · FAIL 0
상시 독립검증 (3선)  응답대기 (`IVR-4F87EE098FF3`)
도전 대상 가정        24건
```


---

# `IVR-3F62C4510DDE` — 검증 요청

`IVR-4F87EE098FF3` 지적 4건(F-701 중부적합 · F-702 · F-703 · F-704)을 시정했다.

## F-701 — 시정이 부분 적용에 그쳤다

지적대로다. **7차 시정 문서에 "9곳에서 같은 방식으로 합산한다"고 적어 놓고 두 곳만
고쳤다.** 같은 팩 안에서 같은 라인명·같은 규정 인용으로 2.41배 차이가 났다.

| 서식 | 라인 | 시정 전 | 시정 후 |
|---|---|---:|---:|
| BR-11 | 3000 | 33,582,430,343.88 | 33,582,430,343.88 |
| B2402 | 4000 | 80,848,614,462.94 | 33,582,430,343.88 |
| B2506 | 4000 | 80,848,614,462.94 | 33,582,430,343.88 |
| B2403 | 1030 | 80,848,614,462.94 | 33,582,430,343.88 (순차액 합계) |
| B2426 | 4020 | 655,118,255.46 | −17,031,459,646.84 (순차액) |

부문 표에 `max(0, ·)`를 걸면 전행에서와 **같은 이유로 또 과대해진다** — 초과적립
부문이 부족 부문을 상계하지 못하기 때문이다. `reserve_net_gap()`으로 부호 있는
순차액을 쓰며, 부문 합이 전행 순차액과 정확히 일치한다. 가계 −170억이 기업 +506억을
상계해 336억이 된다.

## 교차 서식 대사 신설 — 이번 회차의 실질

`FormCheck`는 **한 서식 안에서만** 대사하므로 서식 간 모순을 원리적으로 잡지
못한다. 그 상태로 "검증 1,735건 실패 0"이 나왔다. headline 9종을 등록해 대사하며,
F-701 원형(B2402에 건별 합산 주입)을 넣으면 최대차 472.7억으로 FAIL한다.

3선이 신설한 `ADV-PROC-05`("시정 확인은 고친 곳이 아니라 쓰는 곳 전부에서")를
절차가 아니라 **기계**로 옮긴 것이다. 절차로 두면 또 놓친다 — 이번이 그 증거다.

## F-702 — 주석의 주장이 사실과 달랐다

지적대로 새 BR-11 검증도 양변이 같은 `reserve_requirement()` 반환값에서 나오므로
자료로는 틀릴 수 없다. "실제로 틀릴 수 있는 대사로 바꾼다"는 주석을 정정하고,
**배선 회귀는 잡지만 자료 오류는 못 잡는다**고 명시했다. 자료 오류를 잡는 역할은
교차 서식 대사가 맡는다.

## F-703 — 지표가 자기 사례를 못 본다

지적대로다. 개선을 시도해 "양변이 정확히 같음"을 의심 기준에 넣었더니 **정상 소계
대사도 전부 걸려** 실질 대사가 12%로 나왔다. 반대 방향 오도이므로 되돌렸다.
항진명제 여부는 값이 아니라 표현식 구조의 문제이며, 완전 판별에는 AST 대조가
필요하고 **미구현이다**. 생성 문장에 그 한계를 그대로 적는다.

## F-704 — 부재 통제

지적대로 `CET1Components.expected_loss_shortfall`이 어디서도 설정되지 않았다.
`expected_loss_vs_provisions()`를 신설해 결선했다 (CRE35.3 · CRE40.11 · CRE40.30).
현 자료는 IRB EL 402.4억 < 충당금 945.3억이라 차감 0이지만, EL이 충당금을 넘으면
차감이 산출된다. **"지금 0"과 "통제가 있다"는 다르다.** 대손준비금과 같은 합계
기준 비교이며 익스포저별 `max(0,·)`를 걸지 않았다.

## 3선이 도전해야 할 지점

1. **항진명제 전수 판별** — F-703의 근본 해소는 미완이다. AST 대조가 필요하고
   3선도 같은 결론이었다. 경험적 구분(seed 교차 + 양변 0)이 내 한계다.
2. **교차 서식 대사의 등록 범위** — headline 9종만 등록했다. 등록되지 않은 수치가
   여러 서식에 실리면 여전히 못 잡는다. 어느 수치가 더 등록돼야 하는가.
3. **부문 순차액 표기** — 부문 표에 음수를 그대로 두는 것이 금감원 서식 규약과
   맞는지 확인이 필요하다. 합계 정합을 택했으나 서식이 음수를 허용하지 않으면
   다른 표기가 필요하다.
4. **파생 비중 · B2824 혼합기준** — 이월, 미해소.

## 상태

```
자체검증 (2선)      PASS 60 · WARN 5 · FAIL 0
상시 독립검증 (3선)  응답대기 (`IVR-3F62C4510DDE`)
도전 대상 가정        24건
```


---

# `IVR-11C3372DCB57` — 검증 요청

`IVR-3F62C4510DDE` 지적 3건(F-801 · F-802 · F-803)을 시정했다. 중부적합 0이
유지됐고 게이트는 `조건부`였다 — 저희 코드로 같은 판정을 재현했다.

## F-802 — 같은 유형을 두 회차 연속 반복했다

| 회차 | 지적 | 내용 |
|---|---|---|
| `IVR-4F87EE098FF3` | F-701 | 대손준비금을 BR-11만 고치고 제출 서식 5종을 놓침 |
| `IVR-3F62C4510DDE` | F-802 | EL 차감을 표시용 분해에만 반영하고 실제 비율을 놓침 |

둘 다 **산출 함수는 옳게 만들고 쓰는 곳 전부에 닿게 하지 않은** 것이다. 3선이
`ADV-PROC-05`를 신설한 직후에 같은 유형을 다시 냈다. 앞 회차에 만든 교차 서식
대사는 서식 간 불일치를 잡지만, 이번 건은 **서식이 아니라 파이프라인 내부 배선**
이라 그 통제가 닿지 않았다.

**시정**

- 충당금 단계를 자본보다 **앞으로** 옮겼다. EL 대비 충당금 비교가 자본 차감으로
  이어지려면 충당금이 먼저 있어야 한다.
- 차감을 실제 `CapitalStack`에 적용했다. 검증: 충당금을 0으로 주입하면 CET1이
  **−54.55bp** 움직인다. 이전에는 0.00bp였다.
- 분해 함수는 gross를 차감액만큼 올려 **불변식(net = 입력)을 유지**한다.
  3선이 실증한 BR-02 명세 합계 FAIL(−560억)이 해소된다.

**초과충당금 산입은 적용하지 않았다.** CRE40.30은 임의규정("may")이고, 적용하면
총자본비율이 **+41.8bp 올라간다**. 결함 시정의 부수효과로 비율을 좋게 만드는 것은
시정이 아니라 다른 변경이다. 산입 여지(`surplus_recognised`, 본 실행 392.7억)는
산출·공시만 한다. 이 판단이 과도하게 보수적이면 지적해 달라.

## F-801 — 값은 고쳤으나 산출근거를 갱신하지 않았다

지적대로다. 가장 분명한 것은 `B2426/4020`이 **−170억인데 formula가
"Σ max(0, ·)"**로 음수가 나올 수 없다고 기술한 것이다. 제출 서식 5건의 formula를
실제 산식으로 정정했다 — 전행 규정 금액은 "max(0, 합계 − 합계) — 합계 기준",
부문 라인은 "최저적립액 − 충당금 (부호 있는 순차액 — 음수는 초과적립…)".

`formula`는 감독당국 검토자가 그 숫자를 이해하는 유일한 근거 필드라는 지적을
그대로 받는다. 값만 고치고 근거를 두면 서식이 자기모순으로 읽힌다.

## F-803 — 미등록 교차 서식 수치를 산출해 공시한다

3선이 125종을 셌고 우리 산출은 **143종**이다(레벨 0 전 라인을 보므로 더 넓다).
상위는 자기자본 13곳 · 총 여신 잔액 9곳 · 해외 총여신 9곳 · 당기순이익 7곳.

전부 등록하는 대신 **미등록 목록 자체를 생성해 넘긴다**. 143종을 손으로 등록하면
유지가 안 되고, 등록이 빠진 상태가 보이지 않는 것이 더 위험하다는 3선 권고를
그대로 따랐다. 목록은 `cross_form.unregistered_shared_values()`가 매 실행 만든다.

## 3선이 도전해야 할 지점

1. **초과충당금 산입 판단** — 적용하지 않는 쪽을 택했다(위 사유). 규정 해석상
   적용이 맞다면 지적해 달라.
2. **파이프라인 내부 배선을 지키는 통제** — 교차 서식 대사는 서식 간만 본다.
   F-802 같은 배선 누락을 기계로 잡을 방법이 있는가. 지금은 없다.
3. **항진명제 전수 판별** — AST 대조 필요, 미구현. 이월.
4. **파생 비중 · B2824 혼합기준** — 이월, 미해소.

## 상태

```
자체검증 (2선)      PASS 60 · WARN 5 · FAIL 0
상시 독립검증 (3선)  응답대기 (`IVR-11C3372DCB57`)
도전 대상 가정        25건
```


---

# `IVR-9D5A5DBAE888` — 검증 요청

`IVR-11C3372DCB57` 지적 F-901(중부적합)을 시정했다. **내가 만든 결함이고 지적이
전부 정확하다.**

## F-901 — 시정이 대상 밖을 지웠다

산출근거 문자열(F-801)을 고치면서 중복된 `formula` 인자를 지우는 스크립트를
썼는데, 종료 조건을 `'",'`로 끝나는 줄로 잡아 **대상 밖 3줄을 함께 삼켰다.**

```
삭제됨   citation=_C29,                       ← BF202/4020 의 인용
삭제됨   FormLine("5000", "담보평가액", …)
삭제됨   FormLine("5010", "담보인정액", …)
결과     4020(대손준비금)에 _CRE22(담보 규정)가 붙음
```

복원했다 — 라인 5,894 → **5,896**, 인용 제자리, 검증 1,735건 실패 0.

## 왜 아무 통제도 잡지 못했나 — 세 층

**1. 값 기반 검증은 사라진 라인을 원리적으로 못 본다.** 라인이 없으면 대사에도
참여하지 않는다. 서식검증 1,735건 실패 0 · 교차 서식 대사 9건 전부 PASS ·
자체검증 FAIL 0 · 테스트 1,049 passed로 직전 회차와 **건수·결과가 동일**했다.

**2. 울린 통제를 내가 껐다.** 문서 생성 구간 대조가 라인 수 감소(5,896→5,894)를
FAIL로 알렸는데 값을 갱신하는 것으로 끝냈다. 직전 회차에는 같은 통제가
**증가**(4,000→4,002)를 잡았을 때 원인(BR-26 라인 추가)까지 규명했다 — 이번엔
**감소**인데도 묻지 않았다. 감소가 더 위험한 방향인데 반대로 대했다. 3선 지적의
핵심이 여기다.

**3. 편집 방식.** 정규식·줄 단위 스크립트로 코드를 고쳤고 그 루프가 원인이었다.

## 시정 — 구조 회귀 통제 신설

`tests/test_form_structure.py`. 값이 아니라 **구조**(서식 → 라인코드·라인명)를
기준선에 고정하고 라인이 사라지면 실패한다. F-901 원형 주입 시 2건을 정확히
탐지한다. 대손준비금 라인에 제29조가 아닌 인용이 붙는 것도 함께 막는다.

**값 기반 지문으로는 안 된다.** 값은 매 회차 바뀌어 재고정이 필요하고, 재고정하는
순간 사라진 라인도 함께 승인된다 — 그게 이번에 일어난 일이다. 구조는 의도적으로
바꿀 때만 바뀌므로 기준선 갱신이 별도 명령을 요구하고, 그 diff가 커밋에 남는다.
2층(울린 통제를 끄기)이 구조적으로 어려워진다.

## 3선이 도전해야 할 지점

1. **구조 기준선의 사각** — 라인 존재·라인명은 보지만 `formula`·`source_module`
   변화는 보지 않는다. F-801이 지적한 산출근거 오기는 여전히 못 잡는다.
   기준선에 넣으면 문구를 다듬을 때마다 실패해 거짓 경보가 되므로 넣지 않았다.
   이 절충이 옳은가.
2. **편집 방식의 위험** — 정규식·줄 단위 스크립트로 코드를 고치는 것이 이번
   원인이었다. 사후 탐지는 붙였으나 방식 자체는 그대로다.
3. **항진명제 전수 판별** — AST 대조 필요, 미구현. 이월.
4. **초과충당금 산입(CRE40.30) 판단** — 직전 회차에 물었고 F-902에서 적합
   확인을 받았다. 이월 없음.
5. **파생 비중 · B2824 혼합기준** — 이월, 미해소.

## 상태

```
자체검증 (2선)      PASS 60 · WARN 5 · FAIL 0
상시 독립검증 (3선)  응답대기 (`IVR-9D5A5DBAE888`)
테스트               1,052건 통과 (구조 회귀 3건 신설)
```


---

# `IVR-F66464125AEE` — 검증 요청

`IVR-9D5A5DBAE888` 지적 2건(F-A01 · F-A02)을 시정하고, 3선이 §7 한계로 남긴
인용 전수 점검에 먼저 답했다. 그 과정에서 **결함을 하나 더 찾았다.**

## F-A01 — 통제가 제출 실행을 덮지 않았다

지적대로다. 기준선을 시험 고정일(2026-06-11)에서 만들었는데 제출 실행
(2026-06-30)에는 52라인이 더 있고, 라인명이 날짜를 담으므로 **어떤 기준일에서도
FAIL**한다. 통제를 제출물에 돌리는 선택지 자체가 닫혀 있었다.

`docs_for_run()`이 문서 계층에서 이미 푼 함정을 **테스트 계층에서 재발**시켰다는
지적이 정확하다 — 같은 교훈을 한 계층에만 적용했다.

**시정** — 날짜를 `<date>`로 정규화하고 그 결과 같아진 연속 라인을 하나로 접었다.
월별 영업일 수가 달라 개수까지 맞출 수는 없다. **두 기준일 구조 차이 0** 확인.

**남는 한계** — 일별 계열 **안에서** 라인 하나가 사라지는 것은 못 잡는다. 루프
생성 라인이라 손으로 쓴 라인 소실(F-901의 실제 양상)보다 위험이 낮다고 보고
절충했다.

## F-A02 — 그리고 내 검사가 한 단계 성겼다

지적대로 최저적립액(제1항)과 순차액(제2항)이 `_C29` 상수를 공유했다. 상수를 갈랐다.

더 중요한 것은 **F-901 시정으로 내가 만든 `test_citation_survives_edits`가
"제29조" 포함만 보아 항의 차이를 구분하지 못했다**는 점이다. 조항까지 보도록
강화하니 같은 유형이 **7건 더** 나왔다 — `B2506`은 항 표기가 없었고 `B2431`은
최저적립 판정에 제2항이 붙어 있었다. 3선 지적 2건 포함 **총 9건** 정정.

통제가 한 단계 성기면 그 한 단계만큼 결함이 산다. 내 검사가 통과한다는 것은
근거가 못 된다 — 그 검사가 무엇을 보는지까지 봐야 한다.

## 인용 전수 점검 — 3선 §7 한계에 대한 답

전수 측정: 라인 5,896 · 인용 있음 5,397(91.5%) · **고유 인용 473종** · 국내 규정
인용 중 항 표기 없음 2,907건 · **조·항 혼용 15건**.

항 표기가 없는 것 자체는 결함이 아니다(조 전체가 적용되면 정당). 문제는 473종을
아무도 통독한 적이 없다는 것이었다. **5,397라인은 통독할 수 없지만 473종은 할 수
있으므로** 기준선에 고정했다. 새 인용이 들어오거나 사라지면 실패하고, 기준선
갱신 diff가 곧 "이 인용을 검토했다"는 기록이 된다.

**하지 않는 것을 명시했다** — 인용이 규정 원문과 맞는지는 검증하지 않는다.
규정 텍스트가 저장소에 없다. 통과가 곧 정확성이 아니다.

조·항 혼용은 단언에서 **증분 탐지**로 되돌렸다. 단언으로 두니 "비고"·"한도금액"
같은 일반 라인명이 대량으로 걸렸는데 서식이 다르면 항이 다른 것이 정당하다.
거짓 경보가 나면 다음 사람이 검사를 끈다고 여러 번 적어 놓고 그것을 만들 뻔했다.

## 요청서를 만들다 찾은 것 — stale 방어 세 번째 구멍

시정 후 요청서를 생성했는데 `request_id`가 **그대로였다**. 규정 인용 9건을
고쳤는데도다. 원인이 두 겹이었다.

- 요청 페이로드에 서식 내용을 담는 필드가 하나도 없다.
- `submission_digest` 자체가 `form_id·line_code·value·text_value`만 해시해
  인용·라인명·산식이 바뀌어도 불변이다.

**11차 응답으로 결재가 가능한 상태였다** — 그 응답은 F-A01·F-A02를 지적한
것이고 나는 그것을 고친 상태인데, 고친 사실이 지문에 없었다.

F-301과 같은 유형의 **세 번째** 반복이다.

| 시점 | 지문 대상 | 뚫린 것 |
|---|---|---|
| ~3차 | headline 수치만 | 공시 가정 · 자체검증 WARN |
| 3차 시정 | 여섯 항목 열거 | `recalc_targets`의 `citation` (F-301) |
| 4차 시정 | 요청 객체 전체 | **요청 객체가 서식 내용을 안 담음** |

F-301 때 "요청 전체를 지문화한다"로 고쳤는데 **요청 객체 자체에 서식이 없었다.**
열거를 없앴다고 생각했지만 한 계층 위에서 여전히 열거하고 있었다.

**시정** — `submission_digest`가 라인 속성을 전부 해시하고(라인명·단위·레벨·
산식·인용·산출모듈·소계여부·근거, 서식명·주기·규정근거 포함), 요청서에
`submission_digest` 필드를 신설했다. 확인: `IVR-9D5A5DBAE888` →
**`IVR-F66464125AEE`**.

## 3선이 도전해야 할 지점

1. **"전부 넣는다"의 다음 계층은 어디인가** — 한 계층에 적용하면 그 위나 아래에서
   또 뚫린다는 것이 세 번 확인됐다. 지금 요청서는 서식을 덮지만 산출물 Pack의
   다른 구성물(정규 테이블 CSV·UI·모델카드)은 덮지 않는다. 어디까지가 "제출본"인가.
2. **인용 정합성은 여전히 미검증** — 등록은 열거 가능하게 만들 뿐이다. 규정 원문
   대조가 필요하며 저장소에 규정 텍스트가 없다.
3. **조·항 혼용 15건** — 등록만 했고 정당성은 판단하지 않았다.
4. **일별 계열 내부** — 구조 통제의 절충으로 남는다.
5. **항진명제 전수 판별 · 파생 비중 · B2824 혼합기준** — 이월, 미해소.

## 상태

```
자체검증 (2선)      PASS 60 · WARN 5 · FAIL 0
상시 독립검증 (3선)  응답대기 (`IVR-F66464125AEE`)
테스트               1,056건 통과 (인용 등록 4건 신설)
제출본 지문           9d0c6fa354e2b1e8…
```
