"""quant-validation-agent CLI package.

This package only hosts the command-line entrypoint. The actual quantitative
validation logic lives in `tools/` and `middleware/` at the project root.
"""

__version__ = "0.1.0"
