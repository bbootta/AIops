diff --git a/validation-team-agent/CLAUDE.md b/validation-team-agent/CLAUDE.md
index c64aca6..fa48b61 100644
--- a/validation-team-agent/CLAUDE.md
+++ b/validation-team-agent/CLAUDE.md
@@ -35,6 +35,26 @@
 | 07 통합위기상황분석 | 시나리오→손익·자본 전이, 역위기, 자본계획 연계 |
 | 08 리스크 적합성검증 | 검증 체계 자체 — 프로토콜·통제·재현성 (본 하니스의 자기 부문) |
 
+검증 기준의 **국내 근거**는 두 층이다 — 은행업감독규정 [시행 2026. 4. 1.]이
+경영지도비율 등 임계값을, 은행업감독업무시행세칙 [시행 2026. 6. 30.]이 그
+산정기준을 정한다. 두 원문을 `harness/reference/`에 지문과 함께 보관하고,
+국내 검증 항목은 `harness/domestic_rule_criteria.json`이 관리한다.
+
+`tools.domestic_criteria verify`가 세 가지를 대조한다 — 각 항목의 조문·별표
+인용이 **해당 원문에서 실제로 해석되는지**, 계량 임계의 **원문 발췌가 실재하는지**,
+그리고 하니스 임계가 **규정보다 느슨하지 않은지**. 인용을 원문과 맞춰볼 수 없던
+상태를 두 규정 범위에서 닫은 것이다. 원문이 바뀌면 지문이 어긋나 카탈로그가
+낡았다는 사실이 드러난다.
+
+임계는 규정에, 산정기준은 세칙에 있으므로 한쪽만으로는 국내 기준을 덮지 못한다.
+
+각 부문의 **도메인 업무요건 자체도 적합성검증 기준 항목**이다. BRD Level 1
+요건 131건을 부문·검증관점(데이터·산식·방법론·내부통제·문서화)으로 전개해
+`harness/domain_requirement_criteria.json`이 관리하며, 자동 통제가 있다고
+주장하려면 근거 파일이 실재해야 한다 (`tools.domain_criteria verify`가 강제).
+통제가 없으면 `manual`, 은행 8부문 밖이면 `out_of_scope`로 남기고 사유를 적는다
+— 기준 항목이 있다는 것과 통제가 있다는 것은 다르다.
+
 부문 공통으로 다음을 검증 대상에 포함한다 (검증대상은 통계모형만이 아니다):
 원천데이터·분류·산식·파라미터·코드·업무보고서 서식·수기조정·문서와 주장·
 검증 통제 자체. 신용평가모형(PD·LGD·EAD) 성능·안정성 모니터링과
diff --git a/validation-team-agent/README.md b/validation-team-agent/README.md
index f941361..1a26c7a 100644
--- a/validation-team-agent/README.md
+++ b/validation-team-agent/README.md
@@ -136,6 +136,10 @@ v2 CLI 는 v1 의 `python -m tools.*` 명령을 runpy 로 dispatch 하므로 v1
 | `python -m tools.pack_diff --prev A --curr B` | 두 보고서 팩 간 변화 detection |
 | `python -m tools.pack_verify --pack <dir> [--deep]` | 팩 재현성 자체검증 (입력해시·정책·코드·페이지 재빌드) |
 | `python -m tools.val_coverage report / verify` | PRD-VAL 업무요건 대비 구현 커버리지 (근거 실재성 강제) |
+| `python -m tools.domain_criteria list / report / verify` | 도메인 업무요건 131건 → 적합성검증 기준 항목 — 부문·검증관점별 집계, 자동 통제의 근거 실재성 강제 |
+| `python -m tools.gen_domain_criteria --out <json>` | 기준 항목 원장 생성기 — RYNTA BRD 레지스터가 바뀌면 재실행 (손으로 고치지 않는다) |
+| `python -m tools.domestic_criteria list / report / thresholds / cite-check / verify` | 국내 감독규정(은행업감독규정 + 시행세칙) 검증 항목 — 인용의 원문 해석 + 규정 임계 대 하니스 임계 대조 |
+| `python -m tools.gen_domestic_criteria --out <json>` | 국내 검증 항목 생성기 — 라인 번호를 손으로 적지 않고 원문에서 파생 |
 | `python -m tools.validation_trigger triggers / evaluate / queue` | 상시 모니터링 트리거 평가 → 검증 사례 생성·검토 큐 |
 | `python -m tools.validation_finding open / remediate / reverify / close / queue / lineage / blockers` | Finding 원장 — 재검증 없는 종결 차단·재발 시 중대도 상향 |
 | `python -m tools.validation_memory rounds / patterns / self-defects / carryover / verify` | 검증 기억 원장 — 회차·결함 계보·자기결함·이월을 생성·상호 대조 (손으로 세지 않는다) |
@@ -177,6 +181,8 @@ v2 CLI 는 v1 의 `python -m tools.*` 명령을 runpy 로 dispatch 하므로 v1
 | 임계 설명가능성 attribution | `harness/explainability_attributions.json` | (tools/explainability 게이트) |
 | PRD-VAL 요건 커버리지 | `harness/val_requirement_coverage.json` | (tools/val_coverage verify 게이트) |
 | `harness/regulatory_rule_catalog.json` | 규제 규칙 카탈로그 — 근거수준(국내구속/Basel/내부권고)·원문주기·유효일자(출력하한 경과조치)·RETIRED 차단·의견 코드 5종 매핑 |
+| `harness/domain_requirement_criteria.json` | RYNTA BRD Level 1 도메인 업무요건 131건 → 적합성검증 기준 항목 (부문·검증관점·자동/수동/범위밖 · 근거 실재성 강제) |
+| `harness/domestic_rule_criteria.json` | 국내 감독규정 검증 항목 53건 + 계량 임계 10건 — 근거 원문 2종(`harness/reference/` 은행업감독규정·시행세칙, 지문 고정)에서 인용 해석 및 임계 대조 |
 | `harness/valdoc_coverage.json` | 적합성검증 요건문서(개요서·해설서·BRD v9.6.0 DDOC-VAL) 대비 하니스 커버리지 — v9.6.0 정규 부모 master_ref 연결 |
 | `harness/valdoc_discrepancy_registry.json` | 요건 마스터 자체 불일치 6건(VAL-GAP, PENDING_4_EYES) 참조 사본 — 인지·추적 전용, 해소 권한은 마스터 소유자 |
 | 검증 트리거 원장 | `harness/validation_triggers.json` | (tools/validation_trigger 게이트) |
diff --git a/validation-team-agent/src/vta/cli/__main__.py b/validation-team-agent/src/vta/cli/__main__.py
index bc64327..5247f1b 100644
--- a/validation-team-agent/src/vta/cli/__main__.py
+++ b/validation-team-agent/src/vta/cli/__main__.py
@@ -41,6 +41,8 @@ _DISPATCH = {
     ("report", "digest"): "tools.cro_digest",
     ("pack", "verify"): "tools.pack_verify",
     ("coverage",): "tools.val_coverage",
+    ("criteria",): "tools.domain_criteria",
+    ("domestic",): "tools.domestic_criteria",
     ("trigger",): "tools.validation_trigger",
     ("finding",): "tools.validation_finding",
     ("recalc",): "tools.independent_recalc",
diff --git a/validation-team-agent/tools/cli_index.py b/validation-team-agent/tools/cli_index.py
index 81a233b..fe573b8 100644
--- a/validation-team-agent/tools/cli_index.py
+++ b/validation-team-agent/tools/cli_index.py
@@ -36,6 +36,10 @@ CLI_MODULES: list[tuple[str, str]] = [
     ("tools.cro_digest", "CRO 분기 요약 이메일 초안 생성 (발송 없음 — HITL)"),
     ("tools.pack_verify", "보고서 팩 재현성 자체검증 (입력해시/정책/코드/페이지)"),
     ("tools.val_coverage", "PRD-VAL 업무요건 대비 구현 커버리지 (근거 실재성 검증)"),
+    ("tools.domain_criteria", "도메인 업무요건 → 적합성검증 기준 항목 원장 (근거 실재성 강제)"),
+    ("tools.gen_domain_criteria", "기준 항목 원장 생성기 — 원문 레지스터가 바뀌면 재실행"),
+    ("tools.domestic_criteria", "국내 감독규정 검증 항목 — 인용의 원문 해석·근거 실재성 강제"),
+    ("tools.gen_domestic_criteria", "국내 검증 항목 생성기 — 시행세칙 원문에서 인용을 해석해 생성"),
     ("tools.validation_trigger", "상시 모니터링 트리거 평가 → 검증 사례·검토 큐"),
     ("tools.validation_finding", "Finding 원장 (발견·원인·보완·재검증·종결 계보)"),
     ("tools.independent_recalc", "독립 재계산 + 차이 원인 분해 (VAL-007/008)"),
