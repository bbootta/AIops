# 조건부 결재 — RUN-20260630-42

- 대상 실행: `RUN-20260630-42` (asof 2026-06-30 · seed 42)
- 대상 요청: `IVR-68128ECE5694` (6차)
- 게이트 상태: **`조건부`** → 결재 기록 제출로 **통과 확인**
- 결재 책임자: 인간 검증 책임자
- 결재일: 2026-07-27

> 4차(`IVR-0DC4055ED5AF`) 조건부 결재는 조건 2건이 모두 이행되어 종결됐습니다.
> 본 문서는 그 이후 5·6차를 거친 현재 시점의 결재 기록입니다.

---

## 1. 결재 근거

| 항목 | 결과 |
|---|---|
| 독립 재계산 | **10/10 일치** (차이 0) |
| 중부적합 | **0건** |
| 경부적합 | 2건 (F-501, F-502) |
| 해소·확인 | 4건 (F-503 ~ F-506) |
| 산출값 오류 | **0건** |

6차에 걸친 독립검증에서 제기한 중부적합 4건(F-001 자본 순환 · F-002 CCR·CVA
누락 · F-003 자체검증 공백 · F-101 결함 이전)은 모두 해소됐고, 이번 회차에서도
산출값 오류는 하나도 남지 않았습니다. 남은 2건은 모두 **문서 기재**에 관한
지적이며 산출 결과에는 영향이 없습니다.

이번 회차에서 확인된 실질 개선은 다음과 같습니다.

- **F-503 CVA 단위 정정** — 2선이 `ccr.py`의 반환값이 소요자기자본(K_BA)인데
  파이프라인이 이를 RWA로 그대로 합산하고 있었음을 스스로 찾아 12.5배
  환산했습니다(MAR50.2·RBC20.6). 독립 검산에서
  `460,299,264.19 × 12.5 = 5,753,740,802.40`, 증분 `5,293,441,538.21`이 관측된
  RWA 변화와 정확히 일치하고 `12.5 = 1/0.08`로 환산계수도 정합했습니다.
  **이 오류의 출처는 3선의 1차 지적(F-002)이며, 2차(F-104)에서 3선이 "합계 차이
  0"만 보고 적합 판정했습니다.** 3선 프로토콜의 자기 결함이므로 §5에 별도로
  적었습니다.
- **F-504 서식 확대** — 34건 → 290건, 라인 508 → 5,894, 서식검증 176 → 1,732건
  (실패 0). 5차 지적을 공시가 아니라 빌더를 실제로 채우는 방식으로 해소했습니다.
- **F-505 산출 근거 분류 신설** — 라인마다 실측·파생·혼합·대용·미산출·미영위·
  서술을 판정하며 **미분류 0건 · 미귀속 0건**입니다. 파생을 실측처럼 보이게 하지
  않고 스스로 공시한 점을 기록합니다.

## 2. 인수한 잔여위험

**F-501 — 문서 기재 통계가 코드 사실과 어긋남 (네 번째 반복)**

2선은 "5,842라인 중 실측 68.2%"로 기술했으나 독립 재현 결과
`provenance_frame`은 **5,894라인**, 실측 **4,000건 = 67.87%**입니다
(라인 +52건, 비중 -0.33%p). 같은 실행에서 headline 10종이 비트 단위로 일치하므로
산출 조건 차이가 아니라 문서 수치가 낡았거나 다른 필터로 집계한 것입니다.
서식검증 1,732건(실패 0)은 주장과 정확히 일치하므로 집계 경로 자체는 유효합니다.

개별 영향은 작지만 이 유형은 F-103(문서 수치 오기) · F-201(문서 주장과 코드
불일치) · F-401(가정이 코드보다 낡음)에 이어 **네 번째**입니다. 5차에서 권고한
자체검증 체크가 아직 없어 같은 유형이 계속 재발합니다.

**F-502 — 회차 번호 불일치**

본 요청은 실제로 6차(`IVR-E6BEA5DA0D5F`→`52D8B21C1A1E`→`573F73DBBF35`→
`0DC4055ED5AF`→`AC56730A9613`→`68128ECE5694`)인데 시정 문서와 커밋 제목이 "5차"로
표기했습니다. 요청서에 회차 필드가 없어 게이트 판정에는 영향이 없으나, 시정
문서가 회차로 절을 구성하고 있어 "5차 시정"이 두 번 나타나면 어느 지적에 대한
대응인지 흐려집니다.

**이 두 위험을 안고 갑니다.** 산출값에는 영향이 없으며, 문서를 2차 인용할 때만
발현되는 조건부 위험입니다.

## 3. 후속조건 (이행기한 2026-08-10)

| # | 조건 | 담당 |
|---|---|---|
| 1 | 라인 수·실측 비중 등 기계로 산출 가능한 통계를 문서에 손으로 적지 말고 `provenance_frame`에서 **생성**하도록 전환 — F-102에서 파급 목록에, F-301에서 지문 대상에 이미 적용한 원칙 | 모형개발 |
| 2 | 요청서 가정·시정 문서의 수치를 코드 사실과 대조하는 **자체검증 체크 신설** — 5차 권고 미이행으로 같은 유형이 네 번 재발 | 모형개발 |
| 3 | 회차 표기를 `request_id` 정본에서 파생하거나 표기하지 않음 — "5차"가 두 번 나타나는 상태를 정정 | 업무소유자 |

## 4. 제한 배포 범위

**`RUN-20260630-42` (asof 2026-06-30 · seed 42) 단일 실행 결재에 한정합니다.**
후속 실행·다른 기준일·다른 seed는 본 승인의 범위가 아니며 재검증이 필요합니다.
또한 **문서에 손으로 적힌 산출 근거 통계(라인 수·실측 비중·회차)는 승인 대상이
아니며, 코드 산출값만 결재 범위입니다.**

## 5. 3선 자체 결함과 그 시정

F-503은 3선(적합성검증)의 판정 오류를 3선이 기록한 항목입니다.

- 1차 F-002에서 3선이 "CVA 소요자본 460,299,264.19원을 RWA 합계에 넣으라"고
  요구했습니다.
- 2차 F-104에서 3선이 "합산됐고 차이 0"을 근거로 **적합** 판정했습니다.
- 소요자본과 RWA는 12.5배 차원이 다릅니다. 3선의 독립 재계산이 **합계 일치만
  확인하고 단위·차원 정합성을 확인하지 않았습니다.**

원인은 3선 프로토콜(`harness/adversarial_protocol.json`)의 산식 범주에 단위·차원
challenge가 없던 것입니다. `ADV-CALC-04`(단위·차원 정합성, severity `critical`)를
신설하고 이 사례를 `origin`에 그대로 남겼습니다(challenge 18 → 19, CHG-0153).

> 합계 대사는 단위 오류를 잡지 못한다.

2선이 3선 지적을 그대로 수용하지 않고 자체 검토로 차원 오류를 찾아낸 것은 위임의
방향이 한쪽이 아님을 보여줍니다. 이 점을 결재 근거에 포함합니다.

## 6. 강화 모니터링

- 다음 실행 요청 시 3선이 **문서 기재 통계와 코드 산출값을 전수 대조**합니다
  (라인 수·비중·건수·회차).
- `ADV-CALC-04`를 모든 합산 항목에 적용해 **합계 일치만으로 적합 판정하지
  않습니다.**

## 7. 게이트 통과 검증

결재 기록이 실제로 게이트를 통과하는지 2선 코드(`risk_lib.validation.independent`,
`claude/risk-management-agent-harness-B9Kxm`)로 실행 확인했습니다.

| 시도 | 결과 |
|---|---|
| 본 결재 기록으로 `require(ca)` | **통과** — 조건부 결재 성립 |
| 조건(`conditions`)을 비운 기록 | 거부 — "조건부 승인 기록 미비 — 누락 항목: conditions" |
| 잔여위험(`residual_risk`)을 비운 기록 | 거부 — 누락 항목: residual_risk |
| 이행기한(`due_date`)을 비운 기록 | 거부 — 누락 항목: due_date |
| 배포범위(`scope`)를 비운 기록 | 거부 — 누락 항목: scope |
| 기록 없이 `require()` | 거부 — "독립검증 미완료 … 기록해야 결재 가능" |

게이트 상태는 `조건부`, 사유는 "판정 경부적합 · 중부적합 0건 · 경부적합 2건
(F-501, F-502) — 잔여위험·후속조건·이행기한을 기록해야 결재 가능"입니다.
fail-closed가 유지됩니다. **필수 항목 중 하나라도 비우면 결재되지 않습니다.**

## 8. 기록 위치

- 게이트용 기록: `RUN-20260630-42.conditional_approval.json`
  (`risk_lib.validation.independent.ConditionalApproval` 스키마 대응)
- 검증팀 원장: `tools.conditional_approval` —
  `RUN-20260630-42/IVR-68128ECE5694`, 조건 0/3 이행, 기한 2026-08-10
- 근거 응답: `RUN-20260630-42.response.json` (IVR-68128ECE5694)
- 근거 의견서: `RUN-20260630-42.opinion.md` (6차)
- 직전 결재: `IVR-0DC4055ED5AF` (4차, 조건 2/2 이행 완료) — 커밋 `e7e570c`

## 9. 한계

- 본 문서는 인간 결재 책임자의 판단을 적합성검증 팀에이전트가 **기록**한 것이며,
  판단 자체를 대리하지 않습니다 (CLAUDE.md §7).
- 조건 미이행이 이행기한(2026-08-10)을 초과하면 본 승인의 효력을 재검토해야
  합니다. 검증팀 원장이 기한 초과를 에스컬레이션 대상으로 표시합니다.
- 6차 의견서 §9의 미완료 확인 사항(자기충족 FormCheck 패턴, 규정 인용 오기,
  B2824 혼합기준)은 본 승인으로 해소되지 않았으며 다음 회차 이월 대상입니다.
- 독립검증 범위의 한계(비율형 표준 산출 중심, IRB·FRTB 내부모형 미포함, 합성
  데이터 기준)는 의견서에 기재된 그대로 유효합니다.
